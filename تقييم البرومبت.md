بناء على هذا التقرير في ملف "تقرير_التعارضات_والنواقص.md" أريد عمل تغييرات في ملف "الخطة البسيطة والكاملة.md" لتصبح الأدوار كما في ملف "الادوار الجديدة.md"

طبعا ملف  "الادوار الجديدة.md" تم استيراده من مشروع آخر وفيه بعض الاختلافات عن هذا المشروع خاصة عندما يتحدث عن الكود أو بنية المشروع لذلك أنت ستأخذ منه الفكرة وليس الكود

فقلت لكلود
لأن الأولوية الآن لإنشاء الموقع كاملا والتأكد من الصفحات تعمل وعدم وجود أي مشاكل بعده اكتمال الموقع سيتم إضافة الأدوار
فزيادة الأدوار الآن يزيد من صفحات الاختبار
أي أن الصفحات الخاصة بالمديرة لا حاجة لحذفها الآن
لذلك اطلب ان تكونإضافة الصلاحيات في جلسة مستقبلية  مناسبة بعد الانتهاء من التجربة اليدوية للموقع
و للمعلومية خلال فترة التطوير لايوجد مستخدمين حقيقيين لذلك لا توجد أي خطورة من عملية التأجيل
اكتب برومبت وأشر فيه إلى تقرير الأخطاء والسياق  ومالمطلوب عمله ولماذا. واطلب تعديل الخطة
واقترحت أن يكون التعديل على الجلسة 29

فكتب هذا البرومبت
المطلوب منك قبل أن تنفذ البرومبت أن تعيد قراءة كافة الملفات ذات الصلة ومن ثم تعطيني تقييمك للبرومبت

📝 البرومبت المراد تقييمه:

markdown# 🎯 مهمة: تعديل خطة المشروع - تأجيل نظام الصلاحيات للجلسة 29

## 📚 السياق والمراجع

**المشروع:** منصة شموخ التعليمية v3
**المسار:** C:\Users\memm2\Documents\programming\shomokh3

**الوثائق المطلوب قراءتها:**
1. ✅ **ملف الأدوار الجديدة:** `الادوار الجديدة.md` - النظام المستهدف النهائي
2. ✅ **تقرير الأخطاء:** `تقرير_التعارضات_والنواقص.md` - المشاكل المكتشفة

**الوثائق المطلوب تعديلها:**
1. 📝 `الخطة البسيطة والكاملة.md` - إضافة/تعديل الجلسة 29
2. 📝 `project_status.md` - توثيق القرار الاستراتيجي

---

## 🔍 ملخص تقرير الأخطاء

### التعارضات المكتشفة:
1. **صلاحيات middleware:** TEACHER لديها وصول لـ `/students` و `/programs`
2. **نقص فحص الملكية:** بعض APIs لا تتحقق من ملكية الحلقة
3. **تناقض في البرامج:** الصلاحيات غير متسقة

### توصيات التقرير:
- **أولوية عالية:** تصحيح middleware وإضافة `checkCourseOwnership`
- **أولوية متوسطة:** تحديث التوثيق

---

## 🎭 النظام المستهدف (من الادوار الجديدة.md)

### المعمارية النهائية:
3 أدوار في قاعدة البيانات:
├── ADMIN (مدير النظام)
├── TEACHER (مظلة تشمل 5 أدوار فرعية)
│   ├── ACADEMIC_DIRECTOR (مديرة الأكاديمية)
│   ├── ADMINISTRATIVE (الإدارية)
│   ├── TEACHER (المعلمة)
│   ├── SUPERVISOR (المشرفة التربوية)
│   └── SUPPORT (الدعم الفني)
└── STUDENT (الطالبة)

### آلية التمييز:
- **ليس عبر الأدوار** (role) ← 3 أدوار فقط
- **بل عبر الصلاحيات** (permissions) ← 50+ صلاحية
- **7 واجهات مختلفة** ← حسب الصلاحيات الممنوحة

### مثال من الواقع:
```javascript
// ❌ خطأ: لا يوجد ACADEMIC_DIRECTOR في قاعدة البيانات
if (user.role === 'ACADEMIC_DIRECTOR') { }

// ✅ صحيح: التحقق من الصلاحيات
if (hasPermission(user.permissions, 'programs.create')) { }

🎯 القرار الاستراتيجي
القرار:
تأجيل بناء نظام الصلاحيات الكامل إلى الجلسة 29
المبررات:

الأولوية الحالية: إكمال جميع الصفحات والتجربة اليدوية الشاملة
منطق التأجيل:

بناء نظام الصلاحيات الآن يزيد صفحات الاختبار (7 واجهات بدلاً من 3)
النظام الحالي بسيط (3 أدوار) يسهل الاختبار
النظام المستقبلي معقد (50+ صلاحية) يحتاج تخطيط دقيق


لا خطورة: لا يوجد مستخدمين حقيقيين خلال فترة التطوير
معمارية واضحة: لدينا مرجع كامل في الادوار الجديدة.md

استثناء واحد:
✅ يجب تطبيق checkCourseOwnership في الجلسة التالية

طبقة أمان أساسية ضد الوصول للحلقات غير المملوكة
ستبقى حتى مع نظام الصلاحيات المستقبلي
بسيطة وسريعة التطبيق (1 ساعة)

ما لن نفعله الآن:
❌ لا تعدل middleware (سيُعاد كتابته بالكامل في الجلسة 29)
❌ لا تحذف أي صفحات من الأدوار الحالية
❌ لا تبني نظام صلاحيات معقد الآن

✅ المطلوب منك
1️⃣ تعديل الجلسة 29 في الخطة
الملف: الخطة البسيطة والكاملة.md
ابحث عن الجلسة 29 وعدّلها بالكامل:
markdown### 🔐 الجلسة 29: نظام الصلاحيات المتقدم والأدوار الفرعية

**الهدف:** بناء نظام صلاحيات مرن كامل حسب `الادوار الجديدة.md`

**المدة المتوقعة:** 3-4 أيام

**المعمارية المستهدفة:**
- 3 أدوار في قاعدة البيانات (ADMIN, TEACHER, STUDENT)
- 50+ صلاحية موزعة على 11 فئة
- 7 واجهات مستخدم مختلفة
- التمييز عبر `hasPermission()` وليس `role`

**المهام الأساسية:**

#### 📋 اليوم الأول: البنية التحتية
1. **إعادة كتابة `lib/permissions.ts` بالكامل:**
   - تعريف جميع الصلاحيات (50+ صلاحية موزعة على 11 فئة)
   - وظيفة `hasPermission(permissions, requiredPermission)`
   - الصلاحيات الافتراضية لكل دور فرعي (7 أدوار)
   - معايير التسمية: `category.action` (مثل: `users.create`)

2. **إعادة كتابة `middleware.ts` بالكامل:**
   - ❌ حذف نظام `rolePermissions` القديم (صفحات ثابتة)
   - ✅ استبداله بـ `hasPermission()` لكل مسار
   - مثال: بدلاً من `if (role === 'TEACHER')` → `if (hasPermission(permissions, 'programs.view'))`
   - حماية 7 مسارات مختلفة حسب الصلاحيات

3. **تعديل Prisma Schema:**
```prisma
   model User {
     id           String   @id @default(cuid())
     role         UserRole // ADMIN, TEACHER, STUDENT فقط
     permissions  String[] @default([]) // مصفوفة الصلاحيات
     redirectPath String?  // المسار المخصص (/dashboard/academic-director مثلاً)
   }
🎨 اليوم الثاني: الواجهات السبع

إنشاء 7 صفحات dashboard:

/dashboard/admin (مدير النظام)
/dashboard/academic-director (مديرة الأكاديمية)
/dashboard/administrator (الإدارية)
/dashboard/teacher (المعلمة)
/dashboard/supervisor (المشرفة التربوية)
/dashboard/support (الدعم الفني)
/dashboard/student (الطالبة) ← موجودة مسبقاً


تخصيص كل واجهة:

بطاقات إحصائيات مخصصة حسب الصلاحيات
روابط سريعة مختلفة لكل دور
ألوان وأيقونات مميزة (من الادوار الجديدة.md)



🔒 اليوم الثالث: حماية APIs والصفحات

تعديل جميع APIs:

استبدال if (role === '...') بـ if (!hasPermission(permissions, '...'))
مثال:



typescript     // ❌ قبل
     if (session.user.role !== 'ADMIN') throw new Error('غير مصرح');
     
     // ✅ بعد
     if (!hasPermission(session.user.permissions, 'users.create')) {
       throw new Error('غير مصرح');
     }

حماية الصفحات:

إضافة hasPermission() في كل صفحة تحتاج صلاحية
رسائل خطأ واضحة عند عدم وجود الصلاحية
إخفاء أزرار/عناصر حسب الصلاحيات



🎛️ اليوم الرابع: صفحات خاصة بالمدير

صفحة التبديل بين الأدوار:

المسار: /dashboard/admin/role-switcher
تعرض جميع الأدوار السبعة
المدير يستطيع الدخول بأي دور لاختبار الواجهة


صفحة إنشاء دور جديد:

المسار: /dashboard/admin/create-role
المدير يختار الصلاحيات الافتراضية
يحدد المسار المخصص (/dashboard/custom-role)
يحفظ التكوين في قاعدة البيانات


صفحة إدارة المستخدمين المحدثة:

تعيين الصلاحيات لكل مستخدم
اختيار المسار المخصص
تفعيل/إيقاف صلاحيات محددة



🧪 الاختبار الشامل:

اختبار الأدوار السبعة:

تسجيل دخول بكل دور
التحقق من الواجهة المخصصة
اختبار الصلاحيات (ما يظهر وما لا يظهر)
محاولة الوصول لصفحات غير مصرح بها


اختبار صفحات المدير:

التبديل بين الأدوار
إنشاء دور جديد وتجربته
تعديل صلاحيات مستخدم موجود


اختبار APIs:

محاولة الوصول بدون صلاحية (يجب أن يرفض)
التحقق من رسائل الخطأ
اختبار Edge Cases



معايير النجاح:

✅ 7 واجهات تعمل بشكل صحيح ومخصص
✅ نظام hasPermission() يعمل في جميع الصفحات وAPIs
✅ المدير يستطيع التبديل بين الأدوار بدون مشاكل
✅ المدير يستطيع إنشاء أدوار جديدة
✅ لا توجد ثغرات أمنية (اختبار شامل)
✅ npm run build ينجح بدون errors
✅ التوثيق محدث في project_status.md

الملفات الرئيسية المتأثرة:

lib/permissions.ts (إعادة كتابة كاملة)
lib/unified-permissions.ts (جديد - دوال مساعدة)
middleware.ts (إعادة كتابة كاملة)
prisma/schema.prisma (إضافة permissions و redirectPath)
app/(dashboard)/dashboard/[role]/ (7 مجلدات جديدة)
جميع APIs (تعديل شامل)
جميع الصفحات (إضافة فحوصات الصلاحيات)

المرجع الأساسي:

الادوار الجديدة.md ← الدليل الكامل للنظام المستهدف

ملاحظات مهمة:

⚠️ هذه جلسة كبيرة ومعقدة - قد تحتاج 4 أيام فعلية
⚠️ الاختبار الشامل إلزامي (7 أدوار + APIs + Edge Cases)
⚠️ يجب الرجوع لـ الادوار الجديدة.md عند أي استفسار
⚠️ لا تبدأ هذه الجلسة إلا بعد اكتمال جميع الصفحات الأساسية


---

### 2️⃣ توثيق القرار في project_status.md

**الملف:** `project_status.md`

**أضف قسم جديد بعد آخر تحديث:**
```markdown
## 📋 قرارات استراتيجية مهمة

### تأجيل نظام الصلاحيات للجلسة 29
**تاريخ القرار:** [التاريخ الحالي]

**السياق:**
- اكتشف تقرير `تقرير_التعارضات_والنواقص.md` بعض التعارضات في الصلاحيات الحالية
- التقرير أوصى بإصلاح فوري للـ middleware وإضافة `checkCourseOwnership`

**القرار:**
تأجيل إصلاح الصلاحيات الكامل إلى الجلسة 29، مع استثناء واحد

**المبررات:**
1. الأولوية الحالية: إكمال جميع الصفحات والاختبار اليدوي
2. النظام المستقبلي معقد: 3 أدوار → 50+ صلاحية → 7 واجهات
3. لا خطورة: لا يوجد مستخدمين حقيقيين في فترة التطوير
4. المرجع جاهز: `الادوار الجديدة.md` يحتوي على النظام الكامل

**ما سيُنفذ الآن (الجلسة التالية):**
- ✅ إضافة `checkCourseOwnership` فقط (حماية أساسية للحلقات)
- ✅ تطبيق في 4 APIs رئيسية

**ما سيُؤجل للجلسة 29:**
- ⏸️ إعادة كتابة middleware بالكامل
- ⏸️ بناء نظام الصلاحيات الـ 50+
- ⏸️ إنشاء الواجهات السبع
- ⏸️ صفحات التبديل بين الأدوار وإنشاء أدوار جديدة

**النتيجة المتوقعة:**
- الآن: نظام بسيط (3 أدوار) سهل الاختبار
- الجلسة 29: نظام متقدم (7 واجهات) بعد اكتمال الموقع

**الملفات المرجعية:**
- `تقرير_التعارضات_والنواقص.md` - تحليل المشاكل
- `الادوار الجديدة.md` - النظام المستهدف النهائي

3️⃣ إضافة ملاحظة في الجلسة الحالية
في نفس الملف: project_status.md
ابحث عن آخر جلسة مكتملة وأضف:
markdown**ملاحظة حول الصلاحيات:**
تم تأجيل إصلاح نظام الصلاحيات الشامل إلى الجلسة 29 حسب القرار الاستراتيجي المو  ق أعلاه. الصلاحيات الحالية (3 أدوار بسيطة) كافية لفترة التطوير والاختبار.

🚀 الأولويات الحالية (ما يُنفذ في الجلسة التالية)
✅ أولوية عالية - نفذ فوراً:
إضافة checkCourseOwnership فقط

إنشاء ملف: src/lib/course-ownership.ts
تطبيق في 4 APIs:

/api/attendance/course-attendance
/api/attendance/bulk-mark
/api/enrollment/enrolled-students
/api/dashboard/stats



السبب:

حماية أساسية ضد وصول المعلمة لحلقات لا تملكها
بسيطة وسريعة (ساعة واحدة)
ستبقى كما هي حتى بعد الجلسة 29

⏸️ مؤجل للجلسة 29:

❌ لا تعدل middleware الآن
❌ لا تحذف أي صفحات من الأدوار
❌ لا تبني نظام صلاحيات معقد

🎯 التركيز الحالي:

إكمال جميع الصفحات المطلوبة في الخطة
الاختبار اليدوي الشامل (3 أدوار)
إصلاح أي مشاكل في الواجهات
توثيق دقيق لكل جلسة


📋 معايير البدء بالجلسة 29
لا تبدأ الجلسة 29 إلا بعد:

✅ جميع الصفحات الأساسية (حسب الخطة) مكتملة
✅ الاختبار اليدوي لجميع الأدوار (ADMIN, TEACHER, STUDENT) ناجح
✅ npm run build ينجح بدون errors
✅ لا توجد مشاكل حرجة غير محلولة
✅ التوثيق محدث بكل الجلسات السابقة
✅ checkCourseOwnership مطبّق ويعمل بدون مشاكل

الوقت المتوقع: بعد 3-4 أسابيع من الآن (حسب وتيرة الجلسات)

✅ الخلاصة
القرار:

✅ الجلسة 29 ستكون "نظام الصلاحيات المتقدم والأدوار الفرعية"
✅ نطبق checkCourseOwnership فقط في الجلسة التالية
⏸️ نؤجل إعادة كتابة middleware وبناء الواجهات السبع

المطلوب منك:

تعديل الجلسة 29 في الخطة البسيطة والكاملة.md بالمحتوى أعلاه
إضافة قسم "قرارات استراتيجية" في project_status.md
إضافة ملاحظة في آخر جلسة مكتملة عن تأجيل الصلاحيات

السبب:

الأولوية الحالية: إكمال الموقع واختباره (3 أدوار بسيطة)
المستقبل: نظام متقدم (7 واجهات) بعد اكتمال الأساس
لا خطورة: لا مستخدمين حقيقيين الآن

المرجع الأساسي:

الادوار الجديدة.md ← الدليل الكامل للنظام المستهدف
يجب الرجوع له عند بناء الجلسة 29
