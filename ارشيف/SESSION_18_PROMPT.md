# Session 18: إصلاح الأخطاء الحرجة

## السياق
الجلسة 17 مكتملة بنجاح مع تطبيق التصميم الموحد على 16 صفحة. تم اكتشاف 5 أخطاء حرجة تحتاج إلى إصلاح فوري قبل المتابعة.

## المطلوب: اقرأ الملفات التالية أولاً
1. `AI_CONTEXT.md` - لفهم السياق الكامل للمشروع
2. `PROJECT_TIMELINE.md` - لمعرفة آخر الإنجازات والحالة الراهنة

---

## الأخطاء المكتشفة (5 مشاكل)

### 1. 🔴 صفحات "عن الجمعية" محمية بالخطأ
**المشكلة:**
- الأزرار في الصفحة الرئيسية: "عن الجمعية" / "إنجازاتنا" / "تواصل معنا"
- عند الضغط عليها → ينقل إلى صفحة تسجيل الدخول
- **المفترض:** هذه الصفحات عامة للجميع (بدون تسجيل دخول)

**الملفات المتأثرة:**
- `/about` → يجب أن تكون عامة
- `/about/achievements` → يجب أن تكون عامة
- `/about/contact` → يجب أن تكون عامة
- المشكلة في `src/middleware.ts` - يحمي هذه المسارات بالخطأ

---

### 2. 🔴 صفحة التقارير بدون تصميم
**المشكلة:**
- `/academic-reports` في حساب ADMIN و TEACHER
- لم يتم تطبيق التصميم الموحد (Sidebar + AppHeader + BackButton)
- باقية على التصميم القديم

**المطلوب:**
- تطبيق نفس البنية المستخدمة في الـ16 صفحة الأخرى
- إضافة `AppHeader` مع عنوان "التقارير الأكاديمية"

---

### 3. 🔴 خطأ في صفحة البرامج
**المشكلة:**
```
TypeError: programs.map is not a function
```
- في `/programs` بحساب ADMIN
- السطر 356 في `src/app/programs/page.tsx`
- المتغير `programs` ليس مصفوفة

**السبب المحتمل:**
- الـ API ترجع `{ data: [...] }` لكن الكود يتوقع مصفوفة مباشرة
- أو الـ state الأولي ليس `[]`

**المطلوب:**
- التحقق من شكل البيانات العائدة من `/api/programs`
- إصلاح initialization أو destructuring

---

### 4. 🔴 واجهة الدرجات الموحدة مخفية
**المشكلة:**
- في حساب TEACHER: تم حذف "واجهة الدرجات الموحدة" من Dashboard و Sidebar
- هذه الواجهة هي الأساس وفق الخطة
- الصفحات الاحتياطية (daily-grades, weekly-grades, monthly-grades) ظاهرة لكن الموحدة مخفية
- **ملاحظة:** الإعدادات مفعلة لكن الواجهة لا تظهر

**المطلوب:**
1. إعادة إظهار واجهة الدرجات الموحدة `/grades` في:
   - Dashboard (بطاقة الدرجات)
   - Sidebar (عنصر "الدرجات")
2. ربط ظهورها بالإعدادات (`useUnifiedGrades` من جدول Settings)
3. إذا كانت الإعدادات `useUnifiedGrades = true` → تظهر الواجهة الموحدة فقط
4. إذا كانت `false` → تظهر الصفحات الاحتياطية (daily/weekly/monthly)

**الملفات المتأثرة:**
- `src/app/dashboard/page.tsx` - إضافة بطاقة الدرجات الموحدة
- `src/components/shared/Sidebar.tsx` - إضافة رابط الدرجات الموحدة
- منطق الإعدادات يجب أن يتحكم في الظهور

---

### 5. 🔴 صفحة النقاط السلوكية معطلة
**المشكلة:**
```
ReferenceError: AppHeader is not defined
```
- في `/behavior-points`
- السطر 209 في `src/app/behavior-points/page.tsx`
- مكون `BehaviorPointsContent` يستخدم `AppHeader` بدون استيراد

**السبب:**
- الصفحة تستخدم Suspense wrapper
- المكون الداخلي `BehaviorPointsContent` لا يستورد `AppHeader`
- لكن يحاول استخدامه في الـ JSX

**المطلوب:**
- إضافة `import AppHeader from '@/components/shared/AppHeader';`
- أو تطبيق البنية الموحدة بالكامل (مثل الـ16 صفحة الأخرى)

---

## خطة الإصلاح المقترحة

### المرحلة 1: إصلاح الصفحات العامة (أولوية قصوى)
1. تعديل `src/middleware.ts`:
   - إزالة `/about/*` من المسارات المحمية
   - السماح بالوصول العام للصفحات التعريفية

### المرحلة 2: إصلاح صفحة البرامج
1. فحص `src/app/programs/page.tsx` → السطر 356
2. التحقق من شكل البيانات من `/api/programs`
3. إصلاح `programs.map()` error

### المرحلة 3: إصلاح صفحة النقاط
1. فحص `src/app/behavior-points/page.tsx` → السطر 209
2. إضافة import للـ AppHeader
3. تطبيق البنية الموحدة

### المرحلة 4: تطبيق التصميم على صفحة التقارير
1. تحديث `src/app/academic-reports/page.tsx`
2. إضافة Sidebar + AppHeader + BackButton
3. الحفاظ على الوظائف الحالية

### المرحلة 5: إعادة واجهة الدرجات الموحدة
1. تعديل `src/app/dashboard/page.tsx` - إضافة بطاقة الدرجات
2. تعديل `src/components/shared/Sidebar.tsx` - إضافة رابط الدرجات
3. ربط الظهور بـ `useUnifiedGrades` من Settings

---

## معايير النجاح
- ✅ صفحات /about تفتح بدون تسجيل دخول
- ✅ صفحة /programs تعمل بدون errors
- ✅ صفحة /behavior-points تفتح بشكل طبيعي
- ✅ صفحة /academic-reports بالتصميم الموحد
- ✅ واجهة الدرجات الموحدة ظاهرة في حساب المعلمة (مع التحكم بالإعدادات)
- ✅ `npm run build` ينجح بدون أخطاء
- ✅ التطبيق يعمل في جميع الأدوار (ADMIN/TEACHER/STUDENT)

---

## ملاحظات مهمة
- **لا تغير** الصفحات التي تم إصلاحها في الجلسة 17 (الـ16 صفحة)
- **اتبع** نفس البنية المستخدمة في الجلسة 17:
  ```tsx
  <div className="min-h-screen bg-gray-50 flex">
    <Sidebar />
    <div className="flex-1 lg:mr-72">
      <AppHeader title="..." />
      <div className="p-8">
        <BackButton />
        {/* content */}
      </div>
    </div>
  </div>
  ```
- **اختبر** بعد كل إصلاح
- **وثق** التغييرات في PROJECT_TIMELINE.md عند الانتهاء

---

## ابدأ الآن
اقرأ `AI_CONTEXT.md` و `PROJECT_TIMELINE.md` أولاً، ثم ابدأ بإصلاح الأخطاء بالترتيب.
