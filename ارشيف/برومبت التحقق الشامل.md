🔍 برومبت التحقق الشامل - منصة شموخ v3
التاريخ: 23 نوفمبر 2025
الحالة: بعد الجلسة 18 (React 19 + Server Actions + Optimistic UI)
الهدف: التحقق الصارم من كافة الملفات واكتشاف الأخطاء

📋 السياق الكامل للمشروع
اقرأ هذه الملفات أولاً (إلزامي):
PROJECT_TIMELINE.md - تاريخ الجلسات والتطورات
AI_CONTEXT2.md - التعليمات العامة والقواعد التقنية
Session18_UPGRADE_PLAN.md - الخطة التفصيلية للجلسة 18
‏‏دليل التطوير الشامل.md - الدليل الفني الكامل
🚨 المشاكل المبلغ عنها
1. مشكلة السرعة ⚡
التقرير الأصلي: تم ذكر تحسينات في السرعة في ملف ‏‏دليل التطوير الشامل.md:

"تقليل الكود ~60%"
"تقليل JS bundle ~70%"
"سرعة +40%"
المشكلة: المستخدم لا يشعر بتحسن في السرعة عند استخدام التطبيق

المطلوب فحصه:

هل تم تطبيق Server Components بشكل صحيح؟
هل تم تطبيق Server Actions على جميع العمليات؟
هل تم استبدال جميع fetch() بـ Server Components أو Server Actions؟
هل تم تطبيق revalidate على Server Components؟
هل تم إزالة API Routes القديمة المكررة؟
هل تم تطبيق React.cache() على الاستعلامات في src/lib/data/queries.ts؟
2. مشكلة روابط الصفحات 🔗
التقرير: توجد أخطاء في روابط بعض الصفحات عند الدخول عبر الشريط الجانبي

مثال محدد: من حساب المدير (ADMIN) → عند الضغط على زر "الطالبات" → يخرج من النظام أو يحدث خطأ

المطلوب فحصه:

فحص جميع الروابط في src/components/layout/Sidebar.tsx
التحقق من أن المسارات موجودة فعلياً في src/app/
التحقق من حماية المسارات في src/middleware.ts
التحقق من أن الصفحات المحمية بدور معين (ADMIN/TEACHER/STUDENT) تعمل بشكل صحيح
فحص صفحة الطالبات المرتبطة بدور ADMIN
🔍 منهجية التحقق الشاملة
المرحلة 1: فهم البنية الكاملة للمشروع
1.1 قراءة الملفات الأساسية
# اقرأ كل الملفات التوثيقية أولاً لفهم السياق
- PROJECT_TIMELINE.md
- AI_CONTEXT2.md
- Session18_UPGRADE_PLAN.md
- ‏‏دليل التطوير الشامل.md
- schema.prisma (لفهم قاعدة البيانات)
1.2 فحص البنية الملفية
# افحص هيكل المشروع بالكامل
src/
├── actions/          # Server Actions (يجب أن يكون موجود)
├── app/              # Pages + API Routes
├── components/       # React Components
├── hooks/            # Custom Hooks (قد تكون قديمة)
├── lib/              # Utilities + DB + Auth
├── types/            # TypeScript Types
└── ...
المرحلة 2: التحقق من تطبيق الجلسة 18
2.1 التحقق من Server Actions ✅
الملفات المتوقعة:

src/actions/enrollment.ts
src/actions/attendance.ts
src/actions/grades.ts (إن وجد)
عمليات التحقق:

// لكل Server Action، تأكد من:
1. ✅ يبدأ بـ 'use server'
2. ✅ يستخدم Zod validation (import { z } from 'zod')
3. ✅ يستخدم requireAuth() أو requireRole()
4. ✅ يحتوي على Ownership Check (للعمليات الحساسة)
5. ✅ يستخدم revalidatePath() بعد التعديل
6. ✅ يرجع ActionResponse<T> موحد
7. ✅ معالجة الأخطاء بـ try/catch
مثال للفحص:

// src/actions/enrollment.ts
'use server'; // ✅ موجود؟
import { z } from 'zod'; // ✅ موجود؟
import { requireAuth } from '@/lib/auth-helpers'; // ✅ موجود؟
import { revalidatePath } from 'next/cache'; // ✅ موجود؟
const schema = z.object({...}); // ✅ موجود؟
export async function enrollInCourse(...) {
  const session = await requireAuth(); // ✅ موجود؟
  const parsed = schema.safeParse(...); // ✅ موجود؟
  // ... باقي العملية
  revalidatePath('/enrollment'); // ✅ موجود؟
}
2.2 التحقق من Server Components ✅
الصفحات المتوقع تحويلها:

src/app/programs/page.tsx
src/app/enrollment/page.tsx
src/app/courses/page.tsx (إن وجد)
عمليات التحقق:

// لكل Server Component، تأكد من:
1. ✅ async function Component() - وظيفة async
2. ✅ لا يوجد 'use client' في أول السطر
3. ✅ يستخدم await للاستعلامات المباشرة
4. ✅ يستخدم requireAuth() للحماية
5. ✅ لا يستخدم useState/useEffect/fetch
6. ✅ يحتوي على export const revalidate = <seconds>
7. ✅ يستخدم دوال من src/lib/data/queries.ts
مثال للفحص:

// src/app/programs/page.tsx
// ❌ لا يوجد 'use client'
import { requireAuth } from '@/lib/auth-helpers'; // ✅
import { getPrograms } from '@/lib/data/queries'; // ✅
export default async function ProgramsPage() { // ✅ async
  await requireAuth(); // ✅
  const programs = await getPrograms(); // ✅ استعلام مباشر
  
  return <div>...</div>;
}
export const revalidate = 3600; // ✅ موجود؟
2.3 التحقق من Client Components + useActionState ✅
الملفات المتوقعة:

src/app/enrollment/EnrollmentForm.tsx
src/app/enrollment/EnrollmentList.tsx
src/app/programs/ProgramsList.tsx
عمليات التحقق:

// لكل Client Component يستخدم Server Action:
1. ✅ 'use client' في أول السطر
2. ✅ import { useActionState } from 'react'
3. ✅ const [state, formAction, isPending] = useActionState(...)
4. ✅ <form action={formAction}>
5. ✅ disabled={isPending} على الأزرار
6. ✅ عرض state.error و state.message
المرحلة 3: فحص الأمان 🔒
3.1 التحقق من إزالة testUsers
// src/lib/auth.ts
// ✅ تأكد أنه لا يوجد:
const testUsers = [...]; // ❌ يجب أن يكون محذوف!
// ✅ يجب أن يكون authorize() يستعلم من DB فقط:
async authorize(credentials) {
  const user = await db.user.findUnique({
    where: { userEmail: credentials.email }
  });
  // ...
}
3.2 التحقق من auth-helpers
// src/lib/auth-helpers.ts - يجب أن يكون موجود!
export async function requireAuth() {...}
export async function requireRole(roles: string[]) {...}
export async function requireTeacher() {...}
export async function requireStudent() {...}
export async function requireAdmin() {...}
3.3 التحقق من Middleware
// src/middleware.ts
// ✅ يجب أن يحمي المسارات حسب الدور:
- /dashboard/* - جميع المسجلين
- /teacher/* - TEACHER/ADMIN فقط
- /student/* - STUDENT فقط
- /admin/* أو /programs/* أو /users/* - ADMIN فقط
المرحلة 4: فحص الأداء ⚡
4.1 فحص استخدام React.cache()
// src/lib/data/queries.ts
import { cache } from 'react'; // ✅ موجود؟
export const getPrograms = cache(async () => {...}); // ✅ موجود؟
export const getCoursesByProgram = cache(async (id) => {...}); // ✅ موجود؟
export const getTeacherCourses = cache(async (id) => {...}); // ✅ موجود؟
4.2 فحص API Routes المكررة
المشكلة: إذا بقيت API Routes القديمة بجانب Server Actions، لن نحصل على التحسين الكامل

التحقق:

# ابحث عن API Routes المكررة:
src/app/api/enrollment/request/route.ts  # ❌ يجب حذفه (تم استبداله بـ Server Action)
src/app/api/programs/route.ts            # ❌ يجب حذفه (تم استبداله بـ Server Component)
# API Routes التي يجب أن تبقى (لها استخدامات خاصة):
src/app/api/auth/[...nextauth]/route.ts  # ✅ ضروري لـ NextAuth
4.3 فحص استخدام fetch() في Client Components
// ابحث في جميع ملفات src/app/ عن:
const response = await fetch('/api/...'); // ⚠️ يجب استبداله بـ Server Action
// يجب استبداله بـ:
import { someAction } from '@/actions/something';
const result = await someAction(data);
المرحلة 5: فحص الروابط والتنقل 🔗
5.1 فحص Sidebar.tsx
// src/components/layout/Sidebar.tsx
// لكل رابط، تحقق من:
1. ✅ href يشير إلى مسار موجود في src/app/
2. ✅ الصفحة المستهدفة تحتوي على page.tsx
3. ✅ الصفحة محمية بـ requireAuth() أو requireRole()
4. ✅ الرابط يظهر فقط للأدوار المصرح لها
فحص خاص لزر "الطالبات" من حساب ADMIN:

// في Sidebar.tsx، ابحث عن رابط الطالبات:
{
  href: '/students',        // ✅ تأكد من المسار
  label: 'الطالبات',
  icon: <UsersIcon />,
  roles: ['ADMIN']          // ✅ تأكد من الدور
}
// ثم تحقق من:
1. ✅ src/app/students/page.tsx موجود؟
2. ✅ يحتوي على requireAdmin() أو requireRole(['ADMIN'])؟
3. ✅ src/middleware.ts يحمي /students/* للـ ADMIN فقط؟
5.2 فحص جميع الصفحات المحمية
# قم بعمل قائمة بجميع الصفحات:
src/app/
├── dashboard/page.tsx          - جميع الأدوار
├── programs/page.tsx           - ADMIN فقط
├── enrollment/page.tsx         - STUDENT فقط
├── teacher/page.tsx            - TEACHER/ADMIN
├── students/page.tsx           - ADMIN فقط ⚠️ (تحقق من هذا!)
├── attendance/page.tsx         - TEACHER/ADMIN
└── ...
# لكل صفحة، تحقق من:
1. ✅ تستخدم requireAuth() أو requireRole()
2. ✅ محمية في middleware.ts
3. ✅ الرابط في Sidebar يظهر للدور الصحيح
المرحلة 6: اختبار التدفقات الرئيسية 🧪
6.1 تدفق تسجيل الدخول
// 1. التحقق من صفحة تسجيل الدخول
src/app/login/page.tsx
// ✅ تستخدم الحقل الصحيح: email (ليس userEmail)
// ✅ ترسل لـ NextAuth authorize()
// 2. التحقق من auth.ts
src/lib/auth.ts
// ✅ authorize يستعلم من DB فقط
// ✅ لا testUsers hardcoded
// 3. التحقق من إعادة التوجيه
// ✅ بعد تسجيل الدخول → /dashboard
// ✅ Dashboard يعرض المحتوى المناسب للدور
6.2 تدفق الطالبة: طلب الانضمام
// 1. الطالبة تسجل دخول
// 2. تذهب لـ /enrollment
src/app/enrollment/page.tsx
// ✅ Server Component يجلب البرامج من getPrograms()
// ✅ يمرر البيانات لـ EnrollmentList
// 3. EnrollmentList.tsx
// ✅ Client Component
// ✅ عند اختيار برنامج → يجلب الحلقات
// ⚠️ هل تستخدم fetch() أم Server Action؟
// 4. EnrollmentForm.tsx
// ✅ useActionState(enrollInCourse, ...)
// ✅ عند الإرسال → Server Action يعمل
// ✅ رسائل النجاح/الفشل تظهر
6.3 تدفق المعلمة: تسجيل الحضور
// 1. المعلمة تسجل دخول
// 2. تذهب لـ /attendance
src/app/attendance/page.tsx
// ✅ Server Component أم Client Component؟
// ✅ يجلب الطالبات من getCourseEnrollments()?
// 3. AttendanceTable.tsx (إن وجد)
// ✅ useOptimistic للتحديث الفوري
// ✅ Server Action: markAttendance()
// ✅ التحديث يظهر فوراً قبل الاستجابة
6.4 تدفق المدير: عرض الطالبات ⚠️
// هذا التدفق مهم لأنه يحتوي على الخطأ المبلغ عنه!
// 1. المدير يسجل دخول
// 2. في Dashboard → يضغط على "الطالبات"
src/components/layout/Sidebar.tsx
// ✅ الرابط موجود؟
// ✅ href صحيح؟
// ✅ يظهر فقط للـ ADMIN؟
// 3. يفتح الصفحة
src/app/students/page.tsx  // ⚠️ هل موجود؟
// ✅ requireAdmin() موجود؟
// ✅ يجلب البيانات بشكل صحيح؟
// ✅ لا أخطاء في console؟
// 4. Middleware يسمح بالوصول
src/middleware.ts
// ✅ /students/* محمي لـ ADMIN فقط
المرحلة 7: فحص الأخطاء الشائعة
7.1 أخطاء TypeScript
# شغل Build وتحقق من الأخطاء
npm run build
# ✅ يجب أن ينجح بدون errors
# ⚠️ warnings مقبولة إذا كانت ESLint فقط
7.2 أخطاء Runtime شائعة
// 1. Hook Violations
// ❌ استدعاء hooks بشكل شرطي
if (condition) {
  const data = useSWR(...); // ❌ خطأ!
}
// ✅ الحل
const shouldFetch = condition;
const data = useSWR(shouldFetch ? url : null, fetcher);
// 2. Missing 'use client'
// ❌ استخدام hooks في Server Component
export default function Page() {
  const [state, setState] = useState(); // ❌ خطأ!
}
// ✅ الحل
'use client';
export default function Page() {
  const [state, setState] = useState(); // ✅ صحيح
}
// 3. Session/User undefined
// ❌ عدم التحقق من nullability
const userName = session.user.name; // ❌ قد يكون undefined
// ✅ الحل
const userName = session?.user?.name || 'ضيف';
7.3 مشاكل قاعدة البيانات
// 1. Foreign Key Mismatch
// ❌ استخدام حقل خاطئ
const courses = await db.course.findMany({
  where: { teacher: { userEmail: email } } // ❌ indirect
});
// ✅ الحل
const courses = await db.course.findMany({
  where: { teacherId: session.user.id } // ✅ direct FK
});
// 2. Missing Relations
// تحقق من schema.prisma:
model Course {
  id        String @id
  teacherId String  // ✅ FK موجود؟
  teacher   User @relation(fields: [teacherId], references: [id]) // ✅ العلاقة صحيحة؟
}
📝 التقرير المطلوب
بعد إجراء جميع عمليات التحقق أعلاه، قم بإعداد تقرير شامل يحتوي على:

1. ملخص تنفيذي
## 📊 ملخص التحقق الشامل
**التاريخ:** <التاريخ>
**المدة:** <المدة>
**الملفات المفحوصة:** <العدد>
### النتائج الرئيسية:
- ✅ الجلسة 18 مطبقة بنسبة: ___%
- ⚠️ عدد المشاكل المكتشفة: ___
- 🔴 مشاكل حرجة: ___
- 🟡 مشاكل متوسطة: ___
- 🟢 تحسينات مقترحة: ___
2. تفاصيل المشاكل المكتشفة
## 🔴 مشاكل حرجة (يجب إصلاحها فوراً)
### المشكلة #1: <عنوان المشكلة>
**الملف:** `src/...`
**السطر:** <رقم السطر>
**الوصف:** <وصف تفصيلي>
**التأثير:** <ماذا يحدث للمستخدم؟>
**الحل المقترح:**
```typescript
// الكود الحالي (خاطئ)
...
// الكود المقترح (صحيح)
...
الأولوية: 🔴 حرج / 🟡 متوسط / 🟢 منخفض

### 3. تحليل مشكلة السرعة
```markdown
## ⚡ تحليل مشكلة الأداء
### الملاحظات:
1. Server Components المطبقة: ___/___
2. Server Actions المطبقة: ___/___
3. API Routes المحذوفة: ___/___
4. استخدام React.cache(): ✅ / ❌
5. استخدام revalidate: ✅ / ❌
### التحسينات المحققة فعلياً:
- تقليل الكود: ___%
- تقليل Bundle Size: ___KB (قبل) → ___KB (بعد)
- سرعة التحميل الأولي: ___ms (قبل) → ___ms (بعد)
### الأسباب المحتملة لعدم شعور المستخدم بالتحسن:
1. ___
2. ___
3. ___
4. تحليل مشكلة روابط الصفحات
## 🔗 تحليل مشكلة الروابط
### الروابط المكسورة المكتشفة:
| الرابط | الملف | الدور المطلوب | المشكلة | الحل |
|--------|-------|---------------|---------|------|
| /students | Sidebar.tsx | ADMIN | صفحة غير موجودة | إنشاء الصفحة |
| ... | ... | ... | ... | ... |
### فحص خاص: زر "الطالبات" من حساب المدير
**الملف:** `src/components/layout/Sidebar.tsx`
**الرابط:** `/students`
**المشكلة المكتشفة:** ___
**السبب الجذري:** ___
**الحل المقترح:** ___
5. خطة الإصلاح المقترحة
## 🛠️ خطة الإصلاح
### الأولوية 1 (فوري - خلال 24 ساعة)
- [ ] إصلاح زر الطالبات للمدير
- [ ] إصلاح أي روابط مكسورة أخرى
- [ ] إصلاح مشاكل الأمان الحرجة
### الأولوية 2 (مهم - خلال 3 أيام)
- [ ] استكمال تطبيق Server Actions المتبقية
- [ ] حذف API Routes المكررة
- [ ] تطبيق useOptimistic على باقي الصفحات
### الأولوية 3 (تحسينات - خلال أسبوع)
- [ ] تحسين شاشات التحميل
- [ ] إضافة error boundaries
- [ ] تحسين رسائل الأخطاء
🎯 معايير النجاح
يعتبر التحقق ناجحاً إذا:

✅ جميع الروابط في Sidebar تعمل بشكل صحيح
✅ لا أخطاء في console عند التنقل
✅ npm run build ينجح بدون errors
✅ تسجيل الدخول يعمل لجميع الأدوار
✅ زر الطالبات يفتح الصفحة الصحيحة للمدير
✅ Server Components محسنة بـ revalidate
✅ Server Actions تستخدم Zod validation
✅ لا testUsers في auth.ts
🚀 البدء
للنموذج اللغوي الذي سينفذ هذا البرومبت:

اقرأ جميع ملفات السياق المذكورة في البداية
افحص البنية الكاملة للمشروع (src/)
نفذ جميع عمليات التحقق في المراحل 1-7
اكتب تقرير شامل يحتوي على:
الملخص التنفيذي
قائمة بجميع المشاكل المكتشفة (مع الأولويات)
تحليل تفصيلي لمشكلتي السرعة والروابط
خطة إصلاح واضحة
لا تفترض - تحقق من كل شيء بنفسك
استخدم ذاكرتك السياقية الهائلة لفهم المشروع كاملاً
ملاحظة مهمة: يمكنك قراءة جميع الملفات في المشروع - لا توجد قيود على حجم السياق!

ابدأ الآن! 🚀