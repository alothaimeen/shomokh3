# 🤖 AI Context - منصة شموخ v3

**آخر تحديث:** 22 نوفمبر 2025  
**الغرض:** مرجع تقني ثابت للذكاء الاصطناعي - يُقرأ مرة واحدة في كل جلسة

---

## 📋 نظرة عامة

**Shomokh v3** - منصة إدارة تعليمية للقرآن الكريم
- **Tech Stack:** Next.js 15, TypeScript, Prisma, Supabase PostgreSQL
- **Roles:** ADMIN, TEACHER, STUDENT (3 أدوار فقط)
- **النموذج:** Multi-Tenant (كل جمعية قاعدة بيانات منفصلة)
- **الحالة:** 17.5/36 جلسة مكتملة (~55%)

---

## 🛡️ حالة النظام الحالية (موثقة في assurance_report.md)

### ✅ سلامة البيانات (Data Integrity)
- **قاعدة البيانات:** سليمة 100% ومنطق العلاقات صحيح (بناءً على `DIAGNOSIS_REPORT.md`).
- **لا توجد سجلات يتيمة:** تم التحقق من جميع العلاقات (Students <-> Users <-> Courses).
- **الحسابات:** خوارزميات الدرجات دقيقة وتعمل بشكل صحيح.

### 🚨 الثغرات الأمنية المعروفة (أولوية قصوى)
- **نوع الثغرة:** IDOR (Insecure Direct Object Reference).
- **المواقع المصابة:**
  1. `src/app/api/grades/route.ts`: يسمح بجلب درجات طلاب آخرين.
  2. `src/app/api/enrollment/manage-request/route.ts`: يسمح بقبول طلاب في حلقات لا يملكها المعلم.
- **الحل المطلوب:** عدم الاعتماد على الفلترة في الواجهة الأمامية، وفرض التحقق من الملكية (Ownership Check) في الـ Backend دائماً.

---

## 🎯 القرار الاستراتيجي المحدث

**إعادة البناء الكاملة والتدريجية** بناءً على:
- ✅ **البساطة أولاً** - تجنب كوارث v1 و v2
- ✅ **مراعاة قيود الذكاء الاصطناعي** - مهام صغيرة مكتملة
- ✅ **بروتوكولات أمان ضد الأخطاء** - منع الافتراضات والأسس الناقصة
- ✅ **تطبيق كامل لنظام الدرجات المعقد** - بشكل تدريجي
- ✅ **إضافة جميع الميزات المطلوبة** - من مواصفات منصة شموخ

### 🧭 فلسفة المشروع

- إعادة البناء من الصفر وفق مبدأ "الميزة الواحدة في كل جلسة"
- التبسيط قبل التعقيد، والانتقال من الحلول الصلبة إلى المرنة تدريجياً
- كل قرار تقني يجب أن يدعم سهولة الصيانة، وقابلية النشر لجمعيات متعددة
- أي فكرة جديدة تُعرض أولاً، ثم تُنفذ بعد الموافقة فقط

---

## 🚨 قواعد Supabase الحرجة (لا تجاهل)

### الإعداد الوحيد المقبول
```bash
DATABASE_URL="postgresql://postgres:[password]@db.[project].supabase.co:6543/postgres?pgbouncer=true"
```

### ❌ المحظورات
- Port 5432 (يسبب P1001)
- `prisma db push` (يتجمد)
- `prisma migrate dev` (يتجمد)
- DIRECT_URL (غير مدعوم)

### ✅ الطريقة الصحيحة لتعديل Schema
```typescript
await prisma.$executeRawUnsafe(`CREATE TABLE ...`);
npm run db:setup
```

### بروتوكول الفحص
```bash
✅ DATABASE_URL يحتوي :6543
✅ ينتهي بـ ?pgbouncer=true
✅ اختبر بـ: npx prisma studio
✅ راقب console لأخطاء P1001
```

---

## 🏢 نموذج النشر للجمعيات المتعددة

### الاستراتيجية
```yaml
النموذج: Multi-Tenant Deployments
الهدف: مشروع مفتوح المصدر لكل الجمعيات مجاناً
التقنية: Environment Variables + Docker Support
قواعد البيانات: Supabase (PostgreSQL) + Prisma
```

### الفوائد
```yaml
✅ استقلالية: كل جمعية لها قاعدة بيانات Supabase منفصلة
✅ أمان: لا مشاركة بيانات بين الجمعيات
✅ مرونة: إعدادات منفصلة لكل جمعية
✅ تكلفة: كل جمعية تدير مشروعها الخاص
✅ تخصيص: اسم، شعار، وثيم مستقل
```

### بنية النشر
```
shamokh-v3/
├── .env.example
├── .env.local
├── docker-compose.yml
├── setup-guide.md
└── deployment/
  ├── vercel.json
  ├── netlify.toml
  └── docker/
```

---

## 📏 معايير التسمية (إلزامية)

### camelCase في كل مكان
```typescript
// ✅ صحيح
interface User {
  userName: string;     // NOT name
  userEmail: string;    // NOT email
  passwordHash: string;
  userRole: UserRole;
  isActive: boolean;
}

// ✅ API Response
{ courseName, courseDescription, maxStudents }

// ❌ خطأ
{ name, description, max_students }
```

**السبب:** v1 و v2 فشلا بسبب تناقضات التسمية

---

## 🏗️ الأنماط المعمارية

### 🏗️ قرارات معمارية حاسمة (Cleanup Decisions)
1. **إدارة المهام (Tasks):** 
   - ✅ المعتمد: `src/app/api/points` (نظام النقاط).
   - ❌ المهمل (Zombie Code): `src/app/api/tasks` (يجب حذفه وتجاهله تماماً).
2. **البحث عن الطلاب:**
   - يجب استخدام `userId` دائماً بدلاً من `studentName` لضمان الدقة ومنع تضارب الأسماء.

### 1. NextAuth Authentication
```typescript
// Session Structure
{
  user: {
    id: string;
    name: string;    // maps to userName
    email: string;   // maps to userEmail
    role: UserRole;  // ADMIN | TEACHER | STUDENT
  }
}

// Test Credentials
admin@shamokh.edu / admin123
teacher1@shamokh.edu / teacher123
student1@shamokh.edu / student123
```

### 2. Middleware Pattern
```typescript
const rolePermissions = {
  ADMIN: ['/*'],
  TEACHER: ['/dashboard', '/teacher', '/attendance', ...],
  STUDENT: ['/dashboard', '/enrollment', '/my-grades', ...]
};
```

### 3. API Route Pattern
```typescript
export async function GET(request: NextRequest) {
  // 1. Verify session
  const session = await getServerSession(authOptions);
  if (!session) return NextResponse.json({ error: 'غير مصادق' }, { status: 401 });
  
  // 2. Check role
  if (session.user.role !== 'ADMIN') 
    return NextResponse.json({ error: 'غير مصرح' }, { status: 403 });
  
  // 3. Execute query
  const data = await db.model.findMany();
  
  // 4. Return
  return NextResponse.json({ data });
}
```

### 4. Client Component Pattern
```typescript
'use client';

export default function Page() {
  const { data: session, status } = useSession();
  
  if (status === "loading") return <LoadingSpinner />;
  if (!session) { router.push('/login'); return null; }
  
  // Component logic...
}
```

**قاعدة:** كل صفحة تفاعلية تبدأ بـ `'use client'`

---

## 🗄️ Database Schema

```prisma
User (teachers) ──< Course >── Program
                      │
                      ├── EnrollmentRequest >── Student
                      ├── Enrollment >── Student
                      ├── Attendance >── Student
                      └── DailyGrade >── Student

// AttendanceStatus Enum (محدث 10 نوفمبر)
PRESENT    // ح - حاضرة
EXCUSED    // م - معتذرة
ABSENT     // غ - غائبة
REVIEWED   // ر - راجعت
LEFT_EARLY // خ - خروج مبكر
```

---

## 🎯 المهام الشائعة

### إضافة ميزة جديدة
```bash
1. تحقق من الاتصال: npm run db:generate
2. تعديل Schema: node scripts/setup-database.js
3. إنشاء API: src/app/api/[feature]/route.ts
4. إنشاء صفحة: src/app/[feature]/page.tsx
5. اختبار: npm run dev
```

### الأوامر الأساسية
```bash
npm run dev      # Development server
npm run build    # Production build
npm run db:setup # Initialize database
```

---

## 🚨 الأخطاء الشائعة وحلولها

| الخطأ | السبب | الحل |
|-------|-------|------|
| P1001 | Port 5432 أو URL خطأ | استخدم :6543 + ?pgbouncer=true |
| prisma db push يتجمد | PgBouncer | استخدم $executeRawUnsafe |
| Module not found | حزمة مفقودة | npm install [package] |
| 'use client' missing | Hook في Server Component | أضف 'use client' أول الملف |
| Hydration mismatch | محتوى مختلف | استخدم useEffect |

---

## 🏠 الفرق الحاسم: الصفحة الرئيسية vs Dashboard

**⚠️ تنبيه مهم جداً:**

### الصفحة الرئيسية (Landing Page)
```
الملف: src/app/page.tsx
المسار: http://localhost:3000/
الغرض: صفحة الهبوط الأولى (غير محمية)
المحتوى: معلومات عن جمعية شموخ + دعوة للتسجيل
المستخدمون: الزوار قبل تسجيل الدخول
```

### صفحة Dashboard (لوحة التحكم)
```
الملف: src/app/dashboard/page.tsx
المسار: http://localhost:3000/dashboard
الغرض: لوحة التحكم بعد تسجيل الدخول (محمية)
المحتوى: إحصائيات وروابط حسب الدور
المستخدمون: ADMIN, TEACHER, STUDENT (بعد تسجيل الدخول)
```

**❌ خطأ شائع:** الخلط بين `/` و `/dashboard`
**✅ القاعدة:** الصفحة الرئيسية = `src/app/page.tsx` دائماً

---

## 📂 بنية الملفات الرئيسية

```
src/
├── app/
│   ├── page.tsx          # الصفحة الرئيسية (/) - Landing Page
│   ├── dashboard/        # لوحة التحكم (/dashboard) - بعد تسجيل الدخول
│   ├── api/              # Backend (Server Components)
│   │   ├── attendance/   # 5 endpoints
│   │   ├── enrollment/   # 4 endpoints
│   │   ├── grades/       # Grade APIs
│   │   └── users/        # User CRUD
│   ├── [pages]/          # Frontend (ALL 'use client')
│   ├── layout.tsx        # Root (Server Component)
│   └── page.tsx          # Landing (Server Component)
├── lib/
│   ├── auth.ts           # NextAuth config
│   ├── db.ts             # Prisma client
│   ├── grading-formulas.ts  # صيغ الدرجات
│   └── course-ownership.ts  # Authorization
└── middleware.ts         # Route protection

prisma/schema.prisma      # Database schema (175 lines)
scripts/setup-database.js # DB initialization
```

---

## 🔄 بروتوكول الجلسات

### قبل البدء (إلزامي)
```bash
1. ✅ اقرأ project_status.md - الحالة الحالية
2. ✅ اقرأ الخطة.md - الجلسة المطلوبة
3. ✅ اقرأ هذا الملف - القواعد التقنية
4. ✅ شغّل npm run dev - تأكد أن كل شيء يعمل
5. ✅ اسأل إذا كان هناك غموض
```

### بعد الانتهاء (إلزامي)
```bash
1. ✅ اختبر الميزة الجديدة شاملاً
2. ✅ شغّل npm run build - صفر أخطاء
3. ✅ حدّث project_status.md
4. ✅ commit مع رسالة واضحة
5. ✅ حدد الجلسة التالية
```

---

## 🛡️ بروتوكولات الأمان ضد أخطاء الذكاء الاصطناعي

```yaml
✅ ميزة واحدة محددة لكل جلسة
✅ اختبار فوري وشامل بعد كل تغيير
✅ لا انتقال بدون تأكيد النجاح
✅ commit بعد كل نجاح

✅ لا افتراضات بدون سؤال
✅ توضيح كل غموض قبل التنفيذ
✅ توثيق ما يعمل فقط
✅ مراقبة استهلاك التوكنز باستمرار

✅ كل ميزة مكتملة بالكامل قبل التالية
✅ لا أسس ناقصة أبداً
✅ لا انتقال إذا فشل البناء
✅ إصلاح مشاكل البناء أولاً

### 🧪 بروتوكول التحقق الصارم (من VERIFICATION_PROTOCOL_IMPLEMENTATION)
1. **التشخيص قبل التعديل:** استخدم `scripts/full-system-diagnose.js` للتأكد من سلامة النظام قبل إجراء تغييرات جوهرية.
2. **Zero Trust:** لا تثق أبداً في مدخلات الواجهة الأمامية. تحقق من الـ Session والـ Role والـ Ownership في كل API Route.
```

### 🚨 بروتوكول معالجة أخطاء البناء

```bash
1. ❌ لا تنتقل للجلسة التالية أبداً
2. 🔧 حل مشكلة البناء فوراً (أولوية قصوى)
3. 🔍 تحليل رسائل الخطأ بعناية
4. ✅ إعادة تشغيل npm run build بعد الإصلاح
5. 📝 توثيق المشكلة والحل للمستقبل
6. ✅ commit فقط بعد نجاح البناء
```

### مبادئ العمل
1. **ميزة واحدة في كل مرة** - اختبر قبل التالي
2. **اختبار فوري** - لا تستمر مع أخطاء
3. **راقب Console** - Terminal + Browser
4. **تغييرات تدريجية** - commits صغيرة
5. **عربي أولاً** - كل النصوص بالعربية
6. **لا تتخطى جلسات** - اتبع الخطة بالترتيب

---

## 🛡️ عوامل النجاح الرئيسية

1. **لا تعدل معايير التسمية** - camelCase دائماً
2. **تحقق من Supabase دائماً** - قبل أي عملية DB
3. **استخدم session باستمرار** - NextAuth هو المصدر
4. **التزم بصلاحيات الأدوار** - middleware + APIs
5. **اختبر ببيانات حقيقية** - npm run db:seed
6. **احترم حدود PgBouncer** - لا DDL بـ Prisma
7. **اقرأ التوثيق أولاً** - الخطة + project_status
8. **حدّث التوثيق أخيراً** - project_status.md

---

## 📊 الملفات الواجب قراءتها

### ترتيب القراءة الإلزامي في كل جلسة
1.  **`AI_CONTEXT.md` (هذا الملف):** لفهم القواعد والمعايير الثابتة للمشروع.
2.  **`PROJECT_TIMELINE.md`:** لمعرفة آخر جلسة مكتملة، وتحديد الجلسة التالية المطلوبة، والاطلاع على السجل التاريخي عند الحاجة.

### ملفات مرجعية
- `schema.prisma` - مصدر الحقيقة للبيانات (175 سطر)
- `src/lib/grading-formulas.ts` - صيغ الدرجات
- `المواصفات المستخلصة من الخطة.md` - لفهم منطق العمل التفصيلي عند الحاجة.

### 📂 تقارير مرجعية حديثة (نوفمبر 2025)
1. **`assurance_report.md`**: (هام جداً) الحقيقة النهائية حول الأمان وسلامة البيانات.
2. **`DIAGNOSIS_REPORT.md`**: تفاصيل فحص قاعدة البيانات.
3. **`FIXES_SUMMARY.md`**: ملخص آخر الإصلاحات المطبقة.

---

## 🎯 Multi-Tenant Architecture

```yaml
Model: Multi-Tenant Deployments
Strategy: كل جمعية قاعدة بيانات Supabase منفصلة
Benefits:
  - عزل كامل للبيانات
  - تحكم مستقل لكل جمعية
  - مجاني للمؤسسات الإسلامية
  - قابل للتخصيص (اسم، شعار، ثيم)
```

---

## 💡 الحكمة النهائية (دروس v1 و v2)

```yaml
✅ البساطة انتصرت على التعقيد
✅ الاختبار الفوري منع التراكم
✅ التوثيق البسيط حافظ على الاتساق
✅ الأهداف الواضحة منعت التشتت
✅ التطوير التدريجي ضمن النجاح
✅ Supabase حصرياً أنهى فوضى تعدد التقنيات
✅ التفكير في النشر من البداية منع إعادة العمل
```

---

## 📝 بيانات الاختبار

```typescript
// Test Users (استخدمهم دائماً)
admin@shamokh.edu / admin123
teacher1@shamokh.edu / teacher123
student1@shamokh.edu / student123

// Test Programs
prog-1: برنامج الحفظ المكثف
prog-2: برنامج التجويد المتقدم

// Test Course
course-1: حلقة الفجر (prog-1, teacher-1)
```

---

## 🔧 حالات الطوارئ

### إذا تعطلت الميزة
```bash
1. ❌ لا تتابع للميزة التالية
2. ✅ اختبر في بيئة نظيفة
3. ✅ ابحث عن أبسط حل (hardcode إذا لزم)
4. ✅ اطلب مساعدة بعد 30 دقيقة
5. ✅ فكر في تبسيط الهدف
```

### إذا نفدت التوكنز
```bash
1. ✅ احفظ التقدم فوراً
2. ✅ commit الحالة الحالية
3. ✅ وثّق آخر حالة عاملة
4. ✅ حدد هدف الجلسة القادمة
5. ✅ حدّث project_status.md
```

---

## 💡 الدروس المستفادة (v1 & v2)

```yaml
✅ البساطة > التعقيد
✅ الاختبار الفوري > التراكم
✅ التوثيق البسيط > لا توثيق
✅ الأهداف الواضحة > التشتت
✅ التطوير التدريجي > Big Bang
✅ Supabase حصرياً > دعم متعدد
✅ التخطيط للنشر مبكراً > Afterthought
```

---

**📅 ملف مرجعي ثابت - لا يتغير إلا عند تحديثات تقنية كبرى**  
**🎯 اقرأه في بداية كل جلسة، طبقه أثناء العمل**
