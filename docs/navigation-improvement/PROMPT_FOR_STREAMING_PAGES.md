# 🤖 برومبت لتطبيق Streaming & High-Fidelity Skeletons

استخدم هذا البرومبت عندما تريد تحويل أي صفحة أخرى في المشروع (مثل `students`, `users`, `programs`) لتعمل بنفس طريقة لوحة التحكم السريعة.

---

## البرومبت (انسخ هذا وأرسله لي أو لأي نموذج ذكاء اصطناعي)

```markdown
أريد تحويل الصفحة `[اسم_الصفحة]` (مثلاً: `src/app/(dashboard)/students/page.tsx`) لتستخدم تقنية **Streaming** مع **High-Fidelity Skeletons**، بحيث يظهر هيكل الصفحة فوراً وتتحمل البيانات في الخلفية.

يرجى اتباع الخطوات التالية بدقة:

### 1. تحليل الصفحة
- حدد الأجزاء التي تحتاج لجلب بيانات (Database Calls).
- حدد الأجزاء الثابتة (Header, Titles, Static Buttons).

### 2. إنشاء Async Components
- قم بفصل كل جزء يحتاج بيانات إلى مكون منفصل (Server Component).
- الملفات يجب أن تكون في `src/components/[folder]/[Name]Async.tsx`.
- انقل كود `await db...` من الصفحة الرئيسية إلى هذا المكون.

### 3. إنشاء High-Fidelity Skeletons
- أنشئ مكون Skeleton لكل Async Component في `src/components/[folder]/[Name]Skeleton.tsx`.
- **مهم جداً:** الـ Skeleton يجب أن يطابق التصميم الحقيقي تماماً (نفس الـ Layout، الألوان، الأيقونات، الأزرار).
- استخدم `animate-pulse` فقط على النصوص والأرقام المتغيرة.
- لا تستخدم مربعات رمادية كبيرة تغطي التصميم بالكامل.

### 4. تحديث الصفحة الرئيسية (`page.tsx`)
- احذف جميع استدعاءات قاعدة البيانات (`await db...`) من مستوى الصفحة.
- استدعِ المكونات الجديدة داخل `Suspense`.
- ضع الـ Skeleton المناسب في خاصية `fallback`.
- تأكد أن الـ Header والأجزاء الثابتة خارج الـ Suspense لتظهر فوراً.

### مثال للنتيجة المطلوبة في `page.tsx`:
```tsx
return (
  <>
    <AppHeader title="العنوان" /> {/* يظهر فوراً */}
    
    <Suspense fallback={<StatsSkeleton />}> {/* يظهر الـ Skeleton الملون فوراً */}
      <StatsAsync /> {/* تتحمل البيانات ثم تظهر */}
    </Suspense>

    <Suspense fallback={<TableSkeleton />}>
      <TableAsync />
    </Suspense>
  </>
);
```

يرجى البدء بتحليل الصفحة `[اسم_الصفحة]` وتنفيذ الخطوات.
```

---

## 💡 نصائح إضافية عند الاستخدام

1. **الصفحات المرشحة بقوة:**
   - `/students` (فيها جداول وإحصائيات)
   - `/users` (قائمة المستخدمين)
   - `/programs` (بطاقات البرامج)
   - `/academic-reports` (رسوم بيانية ثقيلة)

2. **قاعدة الـ Skeleton:**
   - إذا كان المكون يحتوي على زر "إضافة جديد"، يجب أن يظهر الزر في الـ Skeleton ويكون غير فعال (أو فعال إذا كان لا يعتمد على البيانات).
   - الألوان الخلفية (Background Colors) يجب أن تظهر فوراً.

3. **الهدف:**
   - المستخدم لا يجب أن يرى "شاشة بيضاء" أبداً.
   - المستخدم لا يجب أن يرى "تغيير مفاجئ" (Layout Shift) عند اكتمال التحميل.
