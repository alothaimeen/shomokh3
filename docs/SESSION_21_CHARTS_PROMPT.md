# 📊 تنفيذ الجلسة 21: رسوم بيانية للأداء والتحليلات

> **الهدف:** تنفيذ الجلسة 21 من `PROJECT_TIMELINE.md` بشكل كامل

## 📋 السياق الأساسي

**🔴 مهم جداً:** اقرأ الملفين التاليين **بالكامل** قبل البدء:

1. **`AI_RULES.md`** - **إلزامي القراءة**
   - القواعد التقنية الصارمة والأنماط (Patterns)
   - معايير الأمان (Ownership Check)
   - بروتوكول ترس الشفرة
   - **استخدم Pattern 1 و Pattern 2 و Pattern 3 الموجودة في الملف**

2. **`PROJECT_TIMELINE.md`**
   - الحالة الحالية للمشروع (الجلسة 20.6 مكتملة)
   - الجلسات السابقة والإنجازات

3. **`VISUAL_IDENTITY_REFERENCE.md`**
   - الألوان الرسمية للمشروع
   - الخطوط والتصميم

---

## 🎯 الهدف من الجلسة

تنفيذ **الجلسة 21 من PROJECT_TIMELINE.md** بإضافة **رسوم بيانية تفاعلية** لعرض:
- أداء الطالبات والتحليلات بشكل بصري واضح
- مقارنات بين الحلقات
- تحليل اتجاهات الأداء
- إحصائيات بصرية في Dashboard

---

## 📊 المتطلبات الوظيفية

### 1. الرسوم البيانية المطلوبة

#### 1.1 في Dashboard المديرة (`AdminDashboard.tsx`)
- **رسم بياني خطي (Line Chart)**: اتجاهات الأداء العام عبر آخر 10 أسابيع
- **رسم بياني دائري (Pie Chart)**: توزيع الطالبات حسب مستويات الأداء (ممتاز، جيد، ضعيف)
- **رسم بياني عمودي (Bar Chart)**: مقارنة متوسط الدرجات بين الحلقات المختلفة

#### 1.2 في Dashboard المعلمة (`TeacherDashboard.tsx`)  
- **رسم بياني خطي (Line Chart)**: تقدم طالبات الحلقة عبر الأسابيع
- **رسم بياني عمودي (Bar Chart)**: توزيع الدرجات في آخر تقييم (يومي/أسبوعي/شهري)

#### 1.3 في Dashboard الطالبة (`StudentDashboard.tsx`)
- **رسم بياني خطي (Line Chart)**: تقدم الطالبة الشخصي عبر الوقت
- **رسم بياني مساحي (Area Chart)**: مقارنة أداء الطالبة مع متوسط الحلقة

### 2. الميزات التفاعلية
- عرض التفاصيل عند التمرير (Tooltips)
- تسميات واضحة بالعربية
- ألوان متسقة مع تصميم المنصة (من `tailwind.config.ts`)

---

## 🛠️ المتطلبات التقنية

### 1. مكتبة الرسوم البيانية
استخدم **Recharts** للأسباب التالية:
- تكامل ممتاز مع React وTypeScript
- تخصيص عالي يتماشى مع تصميم المنصة
- SVG rendering مناسب للطباعة والتصدير
- مناسب لكميات البيانات المتوقعة في المشروع

**التثبيت:**
```bash
npm install recharts
npm install --save-dev @types/recharts
```

### 2. البنية المطلوبة

#### الملفات الجديدة:
```
src/
├── components/
│   ├── charts/
│   │   ├── PerformanceTrendChart.tsx      # رسم خطي للاتجاهات
│   │   ├── PerformanceDistributionChart.tsx  # رسم دائري للتوزيع
│   │   ├── CourseComparisonChart.tsx      # رسم عمودي للمقارنة
│   │   ├── StudentProgressChart.tsx       # رسم شخصي للطالبة
│   │   └── ChartWrapper.tsx               # غلاف موحد للرسوم
│   └── dashboard/
│       └── ChartsSkeleton.tsx              # هيكل التحميل
└── actions/
    └── analytics.ts                        # Server Actions للبيانات
```

### 3. معايير التصميم

#### الألوان الرسمية (من `tailwind.config.ts` و `VISUAL_IDENTITY_REFERENCE.md`):

**الألوان الأساسية للمشروع:**
- `primary-purple`: `#8B5CF6` (البنفسجي الرئيسي)
- `primary-blue`: `#3B82F6` (الأزرق الرئيسي)  
- `secondary-purple`: `#A78BFA` (البنفسجي الفاتح)
- `secondary-blue`: `#60A5FA` (الأزرق الفاتح)

**الألوان للرسوم البيانية:**
- استخدم تدرجات من الألوان الأساسية للمشروع (`primary-purple` و `primary-blue`)
- للمقارنات: استخدم `from-primary-purple to-primary-blue` (gradient)
- تجنب ألوان عشوائية - التزم بالهوية البصرية للمنصة

**ألوان مستويات الأداء** (كما في التقارير الحالية):
- **ممتاز (90%+)**: `bg-green-100 text-green-800` + 🟢
- **جيد جداً (75-89%)**: `bg-blue-100 text-blue-800` + 🟡  
- **جيد (60-74%)**: `bg-yellow-100 text-yellow-800`
- **يحتاج تحسين (<60%)**: `bg-red-100 text-red-800` + 🔴

**الخط:**
- اسم الخط: **Cairo** من Google Fonts
- الأوزان: 400، 600، 700
- الاتجاه: RTL

#### UX Requirements:
- جميع الرسوم تكون **Responsive** (تتكيف مع الشاشات الصغيرة)
- استخدام `ResponsiveContainer` من Recharts
- عرض رسالة "لا توجد بيانات كافية" إذا كانت البيانات أقل من 3 نقاط
- Tooltips بالعربية مع تنسيق RTL

---

## 📐 أفضل الممارسات المطلوبة

### من نتائج البحث:
1. **الوضوح والبساطة**:
   - تجنب ازدحام الرسوم البيانية
   - استخدام نوع الرسم المناسب للبيانات
   - تسميات أفقية واضحة

2. **الأداء**:
   - تحديد عدد نقاط البيانات (لا تعرض أكثر من 10-12 أسبوع في الرسم الواحد)
   - استخدام `React.memo` للمكونات الثقيلة
   - Lazy loading للرسوم غير المرئية مباشرة

3. **السياق التعليمي**:
   - الرسوم يجب أن توفر **رؤى قابلة للتنفيذ** (Actionable Insights)
   - تسهيل فهم التقدم بسرعة
   - تحفيز الطالبات من خلال عرض التحسن

---

## 🔐 متطلبات الأمان والصلاحيات

- جميع البيانات تُجلب عبر **Server Actions** في `src/actions/analytics.ts`
- التحقق من **الملكية** (Ownership Check):
  - المديرة: ترى بيانات جميع الحلقات
  - المعلمة: ترى بيانات حلقاتها فقط
  - الطالبة: ترى بياناتها الشخصية فقط
- استخدام Pattern الموجود في `AI_RULES.md` (Pattern 1: Server Action)

---

## 📋 استعلامات البيانات المطلوبة

### Server Actions في `src/actions/analytics.ts`:

1. **`getPerformanceTrend(filters)`**:
   - البيانات: متوسطات الدرجات الأسبوعية/الشهرية للحلقة أو الطالبة
   - الإرجاع: `{ week: string, average: number }[]`

2. **`getPerformanceDistribution(courseId?)`**:
   - البيانات: عدد الطالبات في كل مستوى أداء
   - الإرجاع: `{ level: string, count: number, percentage: number }[]`

3. **`getCourseComparison()`** (للمديرة فقط):
   - البيانات: متوسط الدرجات لكل حلقة
   - الإرجاع: `{ courseName: string, average: number }[]`

4. **`getStudentProgress(studentId)`**:
   - البيانات: تقدم الطالبة الشخصي عبر الوقت
   - الإرجاع: `{ date: string, score: number, courseAverage: number }[]`

---

## ✅ معايير النجاح

- [ ] جميع الرسوم البيانية المطلوبة تعمل بشكل صحيح
- [ ] البيانات تُجلب بأمان من قاعدة البيانات
- [ ] التصميم متسق مع هوية المنصة
- [ ] الرسوم responsive على جميع الأحجام
- [ ] Tooltips واضحة بالعربية
- [ ] لا توجد أخطاء في Build
- [ ] المقارنات توفر رؤى واضحة ومفيدة

---

## 📝 ملاحظات التنفيذ

1. ابدأ بـ **Dashboard المديرة** أولاً كنموذج
2. استخدم `Suspense` مع Skeletons للرسوم البيانية
3. اختبر مع بيانات حقيقية من السكربت الموجود (`scripts/seed_simulation.js`)
4. تأكد من Build ناجح قبل إنهاء الجلسة

---

## 📋 التحديثات المطلوبة بعد الانتهاء

**🔴 إلزامي:** بعد إتمام الجلسة، قم بتحديث الملفات التالية:

### 1. `PROJECT_TIMELINE.md`
- حدّث حالة الجلسة 21 من `⏳` إلى `✅`
- أضف قسم "Session 21" **في أعلى الملف** بعد "الحالة العامة" مع:
  - التاريخ
  - الإنجازات (الرسوم المُنفّذة)
  - الملفات المُنشأة
  - Build Status
- حدّث "الحالة العامة":
  - الجلسة الحالية: `21 مكتملة`
  - التقدم: احسب النسبة الجديدة (21/35 = ~60%)
  - الجلسة القادمة: `22`

**مثال على التنسيق المطلوب:**
```markdown
## ✅ Session 21 (29 نوفمبر 2025)

### رسوم بيانية للأداء والتحليلات - Performance Charts & Analytics

**الهدف:** إضافة رسوم بيانية تفاعلية للتحليلات.

**الإنجازات:**
1. رسوم بيانية في Dashboard المديرة (Line Chart + Pie Chart + Bar Chart)
2. رسوم بيانية في Dashboard المعلمة (Line Chart + Bar Chart)
3. رسوم بيانية في Dashboard الطالبة (Line Chart + Area Chart)
4. Server Actions للتحليلات (`src/actions/analytics.ts`)

**الملفات المُنشأة:**
- `src/components/charts/PerformanceTrendChart.tsx`
- `src/components/charts/PerformanceDistributionChart.tsx`
- `src/components/charts/CourseComparisonChart.tsx`
- `src/components/charts/StudentProgressChart.tsx`
- `src/components/charts/ChartWrapper.tsx`
- `src/components/dashboard/ChartsSkeleton.tsx`
- `src/actions/analytics.ts`

**النتيجة:**
- ✅ رسوم بيانية تفاعلية تعمل
- ✅ المقارنات واضحة ومفيدة
- ✅ Build ناجح (XX routes)
```


### 2. `CURRENT_STATUS.md`
- حدّث التقدم الحالي
- أضف الميزات الجديدة في "الإنجازات الأخيرة"
- حدّث "المهام النشطة" للجلسة 22

### 3. `PROJECT_MAP.md` (إذا لزم الأمر)
- أضف المجلد الجديد `src/components/charts/`
- أضف الملف الجديد `src/actions/analytics.ts`

### 4. اختبار Build النهائي
```bash
npm run build
```
- تأكد من عدم وجود أخطاء
- سجّل عدد الـ routes في التحديث

---

## 🚫 ما لا يجب فعله

- ❌ لا تضيف مكتبات غير ضرورية (Chart.js، Plotly، إلخ)
- ❌ لا تعرض أكثر من 12 نقطة بيانات في رسم واحد
- ❌ لا تنسَ التحقق من الصلاحيات في Server Actions
- ❌ لا تستخدم Client-side fetching (`useEffect`)
- ❌ لا تضف ميزات معقدة غير مطلوبة (تصدير، تكبير، إلخ)

---

**تاريخ الإنشاء:** 29 نوفمبر 2025  
**الجلسة:** 21  
**الحالة:** 🟡 جاهز للتنفيذ
