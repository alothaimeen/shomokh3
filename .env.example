# 🔧 ملف إعدادات منصة شموخ v3 - قالب للجمعيات
# انسخ هذا الملف إلى .env وأكمل البيانات المطلوبة

# =================================================================================
# 🗄️ إعدادات قاعدة البيانات (اختر واحدة)
# =================================================================================

# 📌 الخيار 1: PostgreSQL محلي أو على خادم
# DATABASE_URL="postgresql://username:password@localhost:5432/shamokh_database"

# 📌 الخيار 2: Supabase (مجاني حتى حد معين)
# DATABASE_URL="postgresql://user:password@db.supabase.co:5432/postgres"

# 📌 الخيار 3: PlanetScale MySQL (مجاني حتى حد معين)
# DATABASE_URL="mysql://user:password@aws.connect.psdb.cloud/database-name?sslaccept=strict"

# 📌 الخيار 4: Neon PostgreSQL (مجاني حتى حد معين)
# DATABASE_URL="postgresql://user:password@ep-name.region.aws.neon.tech/database"

# الإعداد الافتراضي (Prisma Postgres المحلي للتطوير)
DATABASE_URL="prisma+postgres://localhost:51213/?api_key=YOUR_API_KEY_HERE"

# =================================================================================
# 🔐 إعدادات المصادقة
# =================================================================================

# مفتاح سري لـ NextAuth (يجب تغييره لكل جمعية)
# استخدم: openssl rand -base64 32
NEXTAUTH_SECRET="change_this_secret_key_for_your_organization"

# رابط الموقع (غير في الإنتاج)
NEXTAUTH_URL="http://localhost:3000"

# =================================================================================
# 🏢 إعدادات الجمعية (اختياري - للتخصيص)
# =================================================================================

# اسم الجمعية
ORGANIZATION_NAME="جمعية تحفيظ القرآن الكريم"

# شعار الجمعية (مسار الصورة)
ORGANIZATION_LOGO="/logo.png"

# رقم الاتصال
ORGANIZATION_PHONE="+966501234567"

# البريد الإلكتروني
ORGANIZATION_EMAIL="info@example.org"

# =================================================================================
# 📧 إعدادات البريد الإلكتروني (اختياري)
# =================================================================================

# SMTP إعدادات
# EMAIL_SERVER_HOST="smtp.gmail.com"
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER="your-email@gmail.com"
# EMAIL_SERVER_PASSWORD="your-app-password"
# EMAIL_FROM="no-reply@example.org"

# =================================================================================
# 🚀 إعدادات النشر
# =================================================================================

# بيئة التشغيل (development / production)
NODE_ENV="development"

# رابط قاعدة البيانات للبيئة المباشرة (إنتاج)
# PRODUCTION_DATABASE_URL="your_production_database_url_here"

# =================================================================================
# 📝 تعليمات سريعة
# =================================================================================

# 1. انسخ هذا الملف إلى .env
# 2. اختر نوع قاعدة البيانات وأكمل DATABASE_URL
# 3. غير NEXTAUTH_SECRET (استخدم: openssl rand -base64 32)
# 4. أكمل بيانات الجمعية
# 5. شغل الأوامر التالية:
#    npm install
#    npx prisma generate
#    npx prisma db push
#    npm run dev

# 🔗 للمساعدة: راجع دليل النشر في setup-guide.md