# 🚀 دليل رفع المشروع إلى الخادم - منصة شموخ v3

**آخر تحديث:** 20 نوفمبر 2025  
**الإصدار الحالي:** Session 18 (الجلسة 18)

---

## 📋 معلومات الخادم

### بيانات الاتصال
```bash
Host: 191.101.81.33
Port: 22
User: root
Protocol: SSH
```

### أمر الاتصال
```bash
ssh -p 22 root@191.101.81.33
```

---

## 🗄️ قاعدة البيانات (Supabase)

### معلومات الاتصال
```env
DATABASE_URL=postgresql://postgres.nupiaprnfcedpvoybwpt:Iloveshomokh@aws-1-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# Supabase Project Details
NEXT_PUBLIC_SUPABASE_URL=https://nupiaprnfcedpvoybwpt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im51cGlhcHJuZmNlZHB2b3lid3B0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg3Njk0NTIsImV4cCI6MjA3NDM0NTQ1Mn0.8R8RvRywIeze5tiavxO3vR63aptnKvQ_OgfpuGgTA8I
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im51cGlhcHJuZmNlZHB2b3lid3B0Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1ODc2OTQ1MiwiZXhwIjoyMDc0MzQ1NDUyfQ._uHKOfeuctjaPx_UPqAPmWFbclP4Y7CehQ3rCgIaCp8
```

### ⚠️ قواعد Supabase الحرجة
- **المنفذ الإلزامي:** 6543 (ليس 5432)
- **PgBouncer:** يجب أن ينتهي URL بـ `?pgbouncer=true`
- **ممنوع استخدام:** `prisma db push` أو `prisma migrate dev`
- **الطريقة الصحيحة:** `npx prisma generate` فقط

---

## 🔐 إعدادات NextAuth

```env
NEXTAUTH_URL=https://shomokh.alothaimeen.xyz
NEXTAUTH_SECRET=shamokh-v3-production-secret-key-2025-secure
```

---

## 🌐 معلومات الموقع

- **الدومين:** https://shomokh.alothaimeen.xyz
- **المسار على الخادم:** `/var/www/shomokh`
- **مدير العمليات:** PM2 (اسم العملية: `shamokh`)
- **Repository:** https://github.com/alothaimeen/shomokh3.git

---

## 📦 ملف .env الكامل للإنتاج

```env
# Database
DATABASE_URL=postgresql://postgres.nupiaprnfcedpvoybwpt:Iloveshomokh@aws-1-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# NextAuth
NEXTAUTH_SECRET=shamokh-v3-production-secret-key-2025-secure
NEXTAUTH_URL=https://shomokh.alothaimeen.xyz

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://nupiaprnfcedpvoybwpt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im51cGlhcHJuZmNlZHB2b3lid3B0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg3Njk0NTIsImV4cCI6MjA3NDM0NTQ1Mn0.8R8RvRywIeze5tiavxO3vR63aptnKvQ_OgfpuGgTA8I
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im51cGlhcHJuZmNlZHB2b3lid3B0Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1ODc2OTQ1MiwiZXhwIjoyMDc0MzQ1NDUyfQ._uHKOfeuctjaPx_UPqAPmWFbclP4Y7CehQ3rCgIaCp8

# Organization
ORGANIZATION_NAME=منصة شموخ التعليمية v3
ORGANIZATION_LOGO=/logo.png
NODE_ENV=production
```

---

## 🔄 عملية الرفع الكاملة (خطوة بخطوة)

### 1. التحضير المحلي

```bash
# التأكد من commit جميع التغييرات
git add .
git commit -m "وصف التحديث"
git push origin master
```

### 2. الاتصال بالخادم

```bash
ssh -p 22 root@191.101.81.33
cd /var/www/shomokh
```

### 3. سحب التحديثات

```bash
# سحب آخر تحديثات من GitHub
git pull origin master

# تثبيت التبعيات (إذا تغير package.json)
npm install --legacy-peer-deps

# تحديث Prisma Client
npx prisma generate

# حذف build القديم وإعادة البناء
rm -rf .next
npm run build
```

### 4. إعادة تشغيل PM2

```bash
# إعادة تشغيل التطبيق
pm2 restart shamokh

# حفظ الإعدادات
pm2 save

# التحقق من الحالة
pm2 status
pm2 logs shamokh --lines 20
```

### 5. الاختبار

```bash
# اختبار محلي على الخادم
curl -I http://localhost:3000

# اختبار من الخارج
# افتح المتصفح: https://shomokh.alothaimeen.xyz
```

---

## ⚡ أوامر سريعة (نسخ ولصق مباشر)

### رفع كامل من الصفر
```bash
ssh -p 22 root@191.101.81.33 "cd /var/www/shomokh && git pull origin master && npm install --legacy-peer-deps && npx prisma generate && rm -rf .next && npm run build && pm2 restart shamokh && pm2 save"
```

### رفع سريع (بدون npm install)
```bash
ssh -p 22 root@191.101.81.33 "cd /var/www/shomokh && git pull origin master && npx prisma generate && rm -rf .next && npm run build && pm2 restart shamokh"
```

### فحص حالة التطبيق
```bash
ssh -p 22 root@191.101.81.33 "pm2 status && pm2 logs shamokh --lines 20 --nostream"
```

### إعادة تشغيل سريعة
```bash
ssh -p 22 root@191.101.81.33 "cd /var/www/shomokh && pm2 restart shamokh"
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: خطأ P1001 (لا يمكن الاتصال بقاعدة البيانات)
**السبب:** استخدام port 5432 بدلاً من 6543  
**الحل:**
```bash
cd /var/www/shomokh
grep "DATABASE_URL" .env
# تأكد أن الـ URL يحتوي على :6543 وينتهي بـ ?pgbouncer=true
```

### المشكلة: Prisma validation errors
**السبب:** Prisma Client غير محدّث  
**الحل:**
```bash
cd /var/www/shomokh
npx prisma generate
pm2 restart shamokh
```

### المشكلة: خطأ 500 عند تسجيل الدخول
**السبب:** NEXTAUTH_URL خاطئ أو .next قديم  
**الحل:**
```bash
cd /var/www/shomokh
cat .env | grep NEXTAUTH_URL
# يجب أن يكون: NEXTAUTH_URL=https://shomokh.alothaimeen.xyz
rm -rf .next
npm run build
pm2 restart shamokh
```

### المشكلة: التبعيات لا تُثبت (peer dependency errors)
**الحل:**
```bash
npm install --legacy-peer-deps
```

### المشكلة: البورت 3000 مشغول
**الحل:**
```bash
pm2 stop shamokh
pm2 delete shamokh
fuser -k 3000/tcp
pm2 start npm --name shamokh -- start
pm2 save
```

---

## 📝 أوامر PM2 المهمة

```bash
# عرض جميع التطبيقات
pm2 list

# عرض logs مباشرة
pm2 logs shamokh

# عرض آخر 50 سطر من logs
pm2 logs shamokh --lines 50 --nostream

# إعادة تشغيل
pm2 restart shamokh

# إيقاف
pm2 stop shamokh

# بدء
pm2 start shamokh

# حذف
pm2 delete shamokh

# حفظ الإعدادات
pm2 save

# معلومات تفصيلية
pm2 show shamokh

# مراقبة الأداء
pm2 monit
```

---

## 🔧 إعادة إنشاء التطبيق من الصفر

إذا حدثت مشاكل كبيرة وتحتاج البدء من جديد:

```bash
# 1. الاتصال بالخادم
ssh -p 22 root@191.101.81.33

# 2. إيقاف وحذف PM2
pm2 stop shamokh
pm2 delete shamokh

# 3. عمل نسخة احتياطية
cd /var/www
mv shomokh shomokh-backup-$(date +%Y%m%d-%H%M)

# 4. استنساخ من GitHub
git clone https://github.com/alothaimeen/shomokh3.git shomokh
cd shomokh

# 5. إنشاء .env
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres.nupiaprnfcedpvoybwpt:Iloveshomokh@aws-1-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true
NEXTAUTH_SECRET=shamokh-v3-production-secret-key-2025-secure
NEXTAUTH_URL=https://shomokh.alothaimeen.xyz
NEXT_PUBLIC_SUPABASE_URL=https://nupiaprnfcedpvoybwpt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im51cGlhcHJuZmNlZHB2b3lid3B0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTg3Njk0NTIsImV4cCI6MjA3NDM0NTQ1Mn0.8R8RvRywIeze5tiavxO3vR63aptnKvQ_OgfpuGgTA8I
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im51cGlhcHJuZmNlZHB2b3lid3B0Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1ODc2OTQ1MiwiZXhwIjoyMDc0MzQ1NDUyfQ._uHKOfeuctjaPx_UPqAPmWFbclP4Y7CehQ3rCgIaCp8
ORGANIZATION_NAME=منصة شموخ التعليمية v3
ORGANIZATION_LOGO=/logo.png
NODE_ENV=production
EOF

# 6. تثبيت وبناء
npm install --legacy-peer-deps
npx prisma generate
npm run build

# 7. تشغيل بـ PM2
pm2 start npm --name shamokh -- start
pm2 save

# 8. التحقق
pm2 status
curl -I http://localhost:3000
```

---

## 📊 معلومات النظام

### Node.js & npm
```bash
node --version  # v20.19.4
npm --version   # 10.8.2
```

### بنية المجلدات على الخادم
```
/var/www/
├── shomokh/              # المشروع الحالي
├── shomokh-old-backup-20251120/  # نسخة احتياطية
└── html/                 # ملفات HTML الأخرى
```

---

## 🔐 حسابات الاختبار

```
Admin:   admin@shamokh.edu    / admin123
Teacher: teacher1@shamokh.edu / teacher123
Student: student1@shamokh.edu / student123
```

---

## ✅ Checklist قبل الرفع

- [ ] تم commit وpush جميع التغييرات على GitHub
- [ ] تم اختبار المشروع محلياً (`npm run build` ينجح)
- [ ] تم تحديث `PROJECT_TIMELINE.md` بتفاصيل الجلسة
- [ ] إذا تغير schema.prisma، تأكد من استخدام `npx prisma generate`
- [ ] إذا أضيفت تبعيات جديدة، استخدم `npm install --legacy-peer-deps`
- [ ] بعد الرفع، اختبر تسجيل الدخول والصفحات الأساسية

---

## 📞 ملاحظات مهمة

1. **لا تستخدم `prisma db push` أبداً** - يتجمد مع PgBouncer
2. **استخدم `--legacy-peer-deps`** دائماً مع npm install
3. **احذف `.next`** قبل كل build لتجنب cache قديم
4. **تأكد من NEXTAUTH_URL** يطابق الدومين الفعلي
5. **PM2 لا يحدّث environment** تلقائياً - احذف وأعد إنشاء إذا تغير `.env`

---

## 🎯 آخر عملية رفع ناجحة

- **التاريخ:** 20 نوفمبر 2025
- **الجلسة:** Session 18
- **الإصدار:** b281f0e (Git commit)
- **الصفحات:** 65 صفحة
- **حجم Build:** ~99KB First Load JS
- **الحالة:** ✅ يعمل بنجاح

---

**ملاحظة:** احتفظ بهذا الملف محدّثاً بعد كل عملية رفع ناجحة.


الأوامر باختصار
git add .
git commit -m "أضف الوصف"
git push origin master

// أولاً: قم بالاتصال بالخادم
ssh -p 22 root@191.101.81.33

// ثانياً: بعد الدخول إلى الخادم، نفذ الأوامر التالية واحداً تلو الآخر
// الذهاب لمجلد المشروع
cd /var/www/shomokh

// سحب التحديثات
git pull origin master

// تثبيت المكتبات (مهم جداً استخدام --legacy-peer-deps)
npm install --legacy-peer-deps

// تحديث قاعدة البيانات (Prisma)
npx prisma generate

// حذف ملفات البناء القديمة (لضمان نظافة البناء)
rm -rf .next

// بناء المشروع (هذه الخطوة قد تأخذ وقتاً)
npm run build

// إعادة تشغيل التطبيق
pm2 restart shamokh

// حفظ حالة التشغيل
pm2 save