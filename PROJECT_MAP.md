---
title: "PROJECT_MAP"
version: "1.0"
last_updated: "2025-11-26"
status: "stable"
description: "خريطة هيكلية للمشروع توضح الملفات الأساسية وتدفق البيانات"
---

# 🗺️ PROJECT MAP - خريطة المشروع (منصة شموخ v3)

> **ملخص:** هذا الملف يشرح "جغرافيا" المشروع. استخدمه لتعرف أين تضع الملفات الجديدة وكيف ترتبط المكونات ببعضها.

## 📚 فهرس
- [1. الهيكل العام للمجلدات](#1-الهيكل-العام-للمجلدات)
- [2. المجلدات الأساسية بالتفصيل](#2-المجلدات-الأساسية-بالتفصيل)
- [3. تدفق البيانات (Data Flow)](#3-تدفق-البيانات-data-flow)
- [4. ملفات التكوين (Configuration)](#4-ملفات-التكوين-configuration)

---

## 1. الهيكل العام للمجلدات

```
shomokh3/
├── src/
│   ├── app/                 # (App Router) صفحات التطبيق والمسارات
│   ├── components/          # مكونات React (UI)
│   ├── lib/                 # مكتبات مساعدة، اتصال DB، و Auth
│   ├── actions/             # Server Actions (عمليات الكتابة)
│   ├── hooks/               # Custom React Hooks (SWR, etc.)
│   └── types/               # تعريفات TypeScript الموحدة
├── prisma/                  # Database Schema & Migrations
├── public/                  # الصور والملفات الثابتة
├── scripts/                 # سكريبتات الصيانة (Node.js)
└── docs/                    # التوثيق والخطط
```

---

## 2. المجلدات الأساسية بالتفصيل

### 📂 `src/app` (المسارات)
- **`/(dashboard)`**: (Route Group) يحتوي على جميع الصفحات المحمية (بعد تسجيل الدخول).
  - `layout.tsx`: يحتوي على `Sidebar` و `Header`.
  - `/dashboard`: لوحة التحكم الرئيسية.
  - `/users`, `/students`: صفحات الإدارة.
  - `/my-grades`, `/my-attendance`: صفحات الطالبة.
  - `/daily-grades`, `/attendance`: صفحات المعلمة.
- **`/api`**: نقاط النهاية (API Routes) - *يتم استبدالها تدريجياً بـ Server Actions*.
- **`/login`, `/register`**: صفحات عامة (Authentication).
- **`/`**: الصفحة الرئيسية (Landing Page).

### 📂 `src/components` (المكونات)
- **`/shared`**: مكونات عامة (Sidebar, AppHeader, BackButton).
- **`/ui`**: مكونات التصميم الأساسية (Button, Card, Input).
- **`/attendance`**: مكونات خاصة بالحضور.
- **`/assessment`**: مكونات التقييم والدرجات.

### 📂 `src/lib` (المكتبات)
- **`db.ts`**: إعداد Prisma Client (Singleton).
- **`auth.ts`**: إعداد NextAuth.js.
- **`auth-helpers.ts`**: دوال التحقق من الصلاحيات (`requireAuth`).
- **`data/queries.ts`**: استعلامات قاعدة البيانات للقراءة (Server-side).

### 📂 `src/actions` (Server Actions)
- **`enrollment.ts`**: إدارة طلبات الانضمام.
- **`grades.ts`**: حفظ وتعديل الدرجات.
- **`attendance.ts`**: تسجيل الحضور.
- **`users.ts`**: إدارة المستخدمين (Admin).

---

## 3. تدفق البيانات (Data Flow)

### 📥 القراءة (Fetching Data)
1. **Server Component** (الصفحة):
   - يستدعي دالة من `src/lib/data/queries.ts`.
   - أو يستدعي `db.model.findMany` مباشرة.
   - يمرر البيانات إلى المكونات الفرعية.

2. **Client Component** (تحديث فوري):
   - يستخدم `useSWR` مع API Route (للبيانات المتغيرة بسرعة).
   - أو يستقبل البيانات الأولية من الـ Server Component.

### 📤 الكتابة (Mutating Data)
1. **Client Component** (Form):
   - يجمع البيانات.
   - يستدعي **Server Action** من `src/actions/`.
2. **Server Action**:
   - يتحقق من الأمان (`auth`, `role`, `ownership`).
   - يتحقق من صحة البيانات (`zod`).
   - ينفذ العملية في DB (`prisma`).
   - يعيد التحقق (`revalidatePath`) لتحديث الواجهة.

---

## 4. ملفات التكوين (Configuration)

- **`prisma/schema.prisma`**: تعريف جداول قاعدة البيانات (مصدر الحقيقة).
- **`middleware.ts`**: حماية المسارات وتوجيه المستخدمين حسب الدور.
- **`next.config.js`**: إعدادات Next.js.
- **`tailwind.config.ts`**: إعدادات التصميم والألوان.

---
**ملاحظة:** عند إضافة مجلد جديد أو تغيير هيكلي كبير، يجب تحديث هذا الملف.
