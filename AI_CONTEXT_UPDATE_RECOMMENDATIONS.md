# 📝 خطة تحديث ملف السياق (AI_CONTEXT Update Plan)

**تاريخ التقرير:** 22 نوفمبر 2025
**الهدف:** تحديث `AI_CONTEXT.md` ليعكس الحالة الراهنة بدقة بعد عمليات التشخيص، التدقيق الأمني، والنشر الناجح.

---

## 1. معلومات الرأس (Header Information)

### 🔄 ما يجب تعديله:
*   **تاريخ التحديث:** تغييره من `13 نوفمبر 2025` إلى `22 نوفمبر 2025`.
*   **الحالة (Status):** تحديث عدد الجلسات المكتملة. الملف الحالي يقول `11/35`. بناءً على الملفات (`SESSION_17`, `SESSION_18`)، يجب تغييرها إلى `18/35 جلسة مكتملة (~51%)`.

---

## 2. قسم جديد: حالة النظام الأمنية والتشخيصية (System Health & Security)

### ➕ ما يجب إضافته (نص جديد):
يجب إضافة قسم بارز (يفضل بعد "نظرة عامة") بعنوان **"🛡️ حالة النظام الحالية (نوفمبر 2025)"**:

```markdown
## 🛡️ حالة النظام الحالية (موثقة في assurance_report.md)

### ✅ سلامة البيانات (Data Integrity)
- **قاعدة البيانات:** سليمة 100% ومنطق العلاقات صحيح (بناءً على `DIAGNOSIS_REPORT.md`).
- **لا توجد سجلات يتيمة:** تم التحقق من جميع العلاقات (Students <-> Users <-> Courses).
- **الحسابات:** خوارزميات الدرجات دقيقة وتعمل بشكل صحيح.

### 🚨 الثغرات الأمنية المعروفة (أولوية قصوى)
- **نوع الثغرة:** IDOR (Insecure Direct Object Reference).
- **المواقع المصابة:**
  1. `src/app/api/grades/route.ts`: يسمح بجلب درجات طلاب آخرين.
  2. `src/app/api/enrollment/manage-request/route.ts`: يسمح بقبول طلاب في حلقات لا يملكها المعلم.
- **الحل المطلوب:** عدم الاعتماد على الفلترة في الواجهة الأمامية، وفرض التحقق من الملكية (Ownership Check) في الـ Backend دائماً.
```

---

## 3. قسم الهندسة المعمارية (Architecture)

### 🔄 ما يجب تعديله/توضيحه:
في قسم "Database Schema" أو قسم جديد "Architecture Decisions":

### ➕ ما يجب إضافته:
```markdown
### 🏗️ قرارات معمارية حاسمة (Cleanup Decisions)
1. **إدارة المهام (Tasks):** 
   - ✅ المعتمد: `src/app/api/points` (نظام النقاط).
   - ❌ المهمل (Zombie Code): `src/app/api/tasks` (يجب حذفه وتجاهله تماماً).
2. **البحث عن الطلاب:**
   - يجب استخدام `userId` دائماً بدلاً من `studentName` لضمان الدقة ومنع تضارب الأسماء.
```

---

## 4. البروتوكولات (Protocols)

### ➕ ما يجب إضافته إلى "بروتوكولات الأمان ضد أخطاء الذكاء الاصطناعي":
يجب إضافة إشارة صريحة لبروتوكول التحقق الأخير:

```markdown
### 🧪 بروتوكول التحقق الصارم (من VERIFICATION_PROTOCOL_IMPLEMENTATION)
1. **التشخيص قبل التعديل:** استخدم `scripts/full-system-diagnose.js` للتأكد من سلامة النظام قبل إجراء تغييرات جوهرية.
2. **Zero Trust:** لا تثق أبداً في مدخلات الواجهة الأمامية. تحقق من الـ Session والـ Role والـ Ownership في كل API Route.
```

---

## 5. الملفات المرجعية (References)

### 🔄 ما يجب تحديثه في قائمة "الملفات الواجب قراءتها":
أضف الملفات التالية كقراءة إجبارية لفهم السياق الحالي:

```markdown
### 📂 تقارير مرجعية حديثة (نوفمبر 2025)
1. **`assurance_report.md`**: (هام جداً) الحقيقة النهائية حول الأمان وسلامة البيانات.
2. **`DIAGNOSIS_REPORT.md`**: تفاصيل فحص قاعدة البيانات.
3. **`FIXES_SUMMARY.md`**: ملخص آخر الإصلاحات المطبقة.
```

---

## 6. ما يجب حذفه (Deletions)

*   ❌ **أي إشارة إلى "الخوف من تضارب البيانات"**: بما أن التشخيص أثبت سلامة البيانات، يجب تخفيف لهجة التحذير من "تضارب العلاقات" واستبدالها بالتحذير من "الثغرات الأمنية".
*   ❌ **إرشادات `prisma db push` القديمة**: إذا كانت هناك تعليمات قديمة تتعارض مع استخدام `pgbouncer` بشكل صارم، تأكد من أن التعليمات الحالية تؤكد فقط على استخدام `migration` أو `executeRaw` كما هو موضح، وحذف أي طرق بديلة قد تسبب تجميد القاعدة.

---

## 📝 الخلاصة للتنفيذ

لتحديث `AI_CONTEXT.md` بشكل فعال، أنصح بنسخ المحتوى الحالي، ودمج النقاط أعلاه في أماكنها المناسبة، مع التركيز الشديد على **قسم الأمان الجديد** لأنه يمثل التغيير الأخطر في مسار المشروع حالياً.
