# برومبت لـ Claude Code: حذف دور MANAGER

## السياق
مشروع منصة شموخ التعليمية v3 في مرحلة POC. نحتاج حذف دور MANAGER لتبسيط النظام والتركيز على الأدوار الأساسية الثلاثة فقط: ADMIN, TEACHER, STUDENT.

المسار: C:\Users\memm2\Documents\programming\shomokh3

## الهدف
إزالة دور MANAGER بالكامل من المشروع مع الحفاظ على استقرار الأدوار الثلاثة الأخرى.

## التغييرات المطلوبة

### 1. قاعدة البيانات
- حذف أي مستخدمين بدور MANAGER من قاعدة البيانات
- تعديل schema.prisma: حذف MANAGER من enum UserRole
- تشغيل migration للتطبيق على قاعدة البيانات

### 2. نظام المصادقة
- في auth.ts: حذف المستخدم الاختباري manager1@shamokh.edu
- إزالة أي references لدور MANAGER

### 3. Middleware وحماية المسارات
- في middleware.ts: إزالة جميع الشروط التي تتحقق من role === 'MANAGER'
- تحديث قوائم الصلاحيات لتشمل ADMIN, TEACHER, STUDENT فقط

### 4. الصفحات والمكونات
البحث في جميع الملفات عن:
- MANAGER كنص
- role === 'MANAGER' كشرط
- userRole === 'MANAGER'
- أي references أخرى للدور

إزالة أو تعديل هذه المراجع بما يناسب السياق.

### 5. API Routes
البحث في جميع API routes عن فحوصات الصلاحيات التي تتضمن MANAGER وإزالتها.

### 6. TypeScript Types
البحث عن أي type definitions أو interfaces تتضمن MANAGER وتحديثها.

## الاختبار المطلوب بعد التنفيذ
1. npm run build - يجب أن ينجح بدون أخطاء
2. التحقق من عدم وجود TypeScript errors
3. التحقق من عمل تسجيل الدخول للأدوار الثلاثة المتبقية

## ملاحظات هامة
- لا توجد بيانات حقيقية في المشروع
- المستخدمون الحاليون اختباريون فقط
- الهدف: تبسيط النظام في مرحلة POC

## الأوامر المتوقعة
بعد التعديلات، قد تحتاج تشغيل:
```
npx prisma migrate dev --name remove_manager_role
npm run build
```

قم بتنفيذ هذه التغييرات بالترتيب المناسب وأخبرني بالنتيجة والتغييرات التي تمت.