# 🔍 برومبت: البحث عن وإزالة جميع آثار دور MANAGER من الكود

## 📋 السياق الكامل

### الوضع الحالي:
- تم حذف دور المدير الأكاديمي (MANAGER) من النظام في 29 سبتمبر 2025
- تم تحديث الكود الأساسي (Schema, APIs, Pages الرئيسية)
- تم تحديث الوثائق بالكامل
- **المشكلة:** لا تزال هناك آثار لدور MANAGER في بعض الملفات

### الأدوار الحالية (3 أدوار فقط):
```yaml
ADMIN: المدير - صلاحيات كاملة (إدارية + أكاديمية + فنية)
TEACHER: المعلمة - إدارة حلقاتها وطالباتها فقط
STUDENT: الطالبة - عرض بياناتها وتسجيل المهام
```

### الآثار المكتشفة:
1. **صفحة تسجيل الدخول** (`src/app/login/page.tsx`): زر "الدخول بالمدير الأكاديمي" لا يزال موجوداً
2. **لوحة التحكم** (`src/app/dashboard/page.tsx`): النص "لوحة تحكم المدير الأعلى" بدلاً من "لوحة تحكم المدير"
3. **احتمال وجود آثار أخرى** في ملفات لم يتم فحصها

---

## 🎯 المطلوب

### المهمة الرئيسية:
البحث الشامل والدقيق عن **جميع الإشارات** لدور المدير الأكاديمي (MANAGER) في الكود وإزالتها بالكامل

### نطاق البحث:

#### 1. البحث النصي عن الكلمات التالية:
```bash
MANAGER          # في الكود (enum, arrays, checks)
Manager          # في النصوص والتعليقات
manager          # في المتغيرات
المدير الأكاديمي    # في النصوص العربية
مدير أكاديمي       # في النصوص العربية
المدير الأعلى      # نص غير صحيح (يجب أن يكون "المدير" فقط)
manager@         # في بيانات الاختبار
```

#### 2. أنماط الكود المشبوهة:
```typescript
// أمثلة على patterns يجب البحث عنها:
['ADMIN', 'MANAGER']              // في arrays
userRole === 'MANAGER'            // في conditions
case 'MANAGER':                   // في switch statements
MANAGER: {                        // في objects
role: 'MANAGER'                   // في data
"MANAGER"                         // في strings
```

#### 3. الملفات ذات الأولوية للفحص:
```yaml
أولوية عالية جداً:
  - src/app/login/page.tsx               # صفحة تسجيل الدخول
  - src/app/dashboard/page.tsx           # لوحة التحكم
  - src/app/register/page.tsx            # صفحة التسجيل (إن وجدت)
  - src/lib/auth.ts                      # المصادقة
  - src/middleware.ts                    # الحماية

أولوية عالية:
  - src/app/**/page.tsx                  # جميع صفحات Frontend
  - src/app/api/**/route.ts              # جميع API routes
  - src/components/**/*.tsx              # المكونات المشتركة
  - prisma/schema.prisma                 # Schema

أولوية متوسطة:
  - src/types/**/*.ts                    # أنواع TypeScript
  - scripts/**/*.js                      # السكريبتات
  - *.md                                 # ملفات التوثيق (تم تحديثها لكن تحقق مرة أخرى)

ملفات للتجاهل:
  - node_modules/
  - .next/
  - .git/
```

---

## 🔧 خطة العمل المطلوبة

### الخطوة 1: البحث الشامل
```bash
استخدم الأدوات التالية للبحث:
1. Grep للبحث عن "MANAGER" في جميع الملفات
2. Grep للبحث عن "المدير الأكاديمي" في جميع الملفات
3. Grep للبحث عن "المدير الأعلى" في جميع الملفات
4. Grep للبحث عن "manager@" في جميع الملفات
5. Grep للبحث عن "Manager" في جميع الملفات
```

### الخطوة 2: التحليل
```yaml
لكل نتيجة بحث:
  - حدد نوع الملف والسياق
  - حدد إذا كانت الإشارة تحتاج إلى حذف أو تعديل
  - أعط أولوية للتعديلات (حرجة، مهمة، ثانوية)
```

### الخطوة 3: التعديلات المطلوبة

#### أ. صفحة تسجيل الدخول (src/app/login/page.tsx):
```typescript
// المطلوب:
// 1. حذف زر "الدخول بالمدير الأكاديمي" بالكامل
// 2. تحديث قائمة أزرار الحسابات التجريبية لتكون 3 فقط:
//    - admin@shamokh.edu (المدير)
//    - teacher1@shamokh.edu (المعلمة سارة)
//    - student1@shamokh.edu (الطالبة فاطمة)
// 3. تحديث النصوص لتكون "المدير" بدلاً من "الآدمن" أو "المدير الأعلى"
```

#### ب. لوحة التحكم (src/app/dashboard/page.tsx):
```typescript
// المطلوب:
// 1. تغيير "لوحة تحكم المدير الأعلى" إلى "لوحة تحكم المدير"
// 2. التأكد من عدم وجود أي إشارة لـ MANAGER في الكود
// 3. التأكد من أن switch statement يحتوي فقط على 3 cases:
//    - ADMIN
//    - TEACHER
//    - STUDENT
```

#### ج. أي ملف آخر يحتوي على MANAGER:
```typescript
// المطلوب:
// 1. إذا كان في enum: احذفه
// 2. إذا كان في array: احذفه من الـ array
// 3. إذا كان في condition: غيّر الشرط ليناسب الأدوار الثلاثة
// 4. إذا كان في switch: احذف الـ case بالكامل
// 5. إذا كان في بيانات تجريبية: احذف السجل بالكامل
// 6. إذا كان في نص عربي: غيّر النص ليعكس الأدوار الحالية
```

---

## 📝 التقرير المطلوب

بعد إكمال البحث والتعديلات، قدم تقريراً يحتوي على:

### 1. نتائج البحث:
```yaml
عدد الملفات التي تحتوي على "MANAGER": X ملف
عدد الملفات التي تحتوي على "المدير الأكاديمي": X ملف
عدد الملفات التي تحتوي على "المدير الأعلى": X ملف
قائمة كاملة بالملفات والأسطر المتأثرة
```

### 2. التعديلات المنفذة:
```yaml
الملف: src/app/login/page.tsx
  السطر: XX
  قبل: [كود قديم]
  بعد: [كود جديد]
  السبب: [سبب التعديل]

الملف: src/app/dashboard/page.tsx
  السطر: XX
  قبل: [كود قديم]
  بعد: [كود جديد]
  السبب: [سبب التعديل]

[... وهكذا لكل تعديل]
```

### 3. الاختبارات:
```bash
✅ npm run dev - يعمل بدون أخطاء
✅ npm run build - ينجح بدون errors
✅ تسجيل الدخول بالأدوار الثلاثة - يعمل
✅ لوحة التحكم لكل دور - تعرض المحتوى الصحيح
✅ لا توجد references لـ MANAGER في console
```

### 4. Git Commit:
```bash
عنوان الـ commit: "fix: إزالة جميع آثار دور MANAGER من Frontend"
رسالة الـ commit: [رسالة تفصيلية]
الملفات المعدلة: [قائمة الملفات]
```

---

## ⚠️ ملاحظات مهمة

### احذر من:
1. **عدم حذف ملفات documentation** - فقط حدّثها
2. **عدم كسر الـ TypeScript types** - تأكد من التوافق
3. **عدم إزالة comments مفيدة** - فقط حدّث المحتوى
4. **عدم كسر الـ authentication flow** - اختبر كل تعديل

### تأكد من:
1. ✅ جميع النصوص العربية محدثة ودقيقة
2. ✅ جميع بيانات الاختبار تعكس الأدوار الثلاثة فقط
3. ✅ لا توجد أخطاء TypeScript
4. ✅ جميع الصلاحيات تعمل بشكل صحيح
5. ✅ UX سلس وواضح للمستخدم

---

## 🎯 معايير النجاح النهائية

```yaml
✅ لا يوجد أي ذكر لـ MANAGER في الكود النشط
✅ لا يوجد أي ذكر لـ "المدير الأكاديمي" في الواجهات
✅ النصوص العربية دقيقة ("المدير" وليس "المدير الأعلى")
✅ صفحة تسجيل الدخول تعرض 3 حسابات فقط
✅ لوحة التحكم تعرض العناوين الصحيحة
✅ npm run build ينجح بدون warnings
✅ جميع الاختبارات اليدوية تنجح
✅ Git commit نظيف ومنظم
```

---

## 📌 ملاحظة أخيرة

هذا البحث والتنظيف **ضروري جداً** لضمان:
- عدم وجود confusion للمستخدمين
- عدم وجود أخطاء مستقبلية
- كود نظيف ومتسق 100%
- تجربة مستخدم واضحة ودقيقة

**ابدأ بالبحث الشامل، ثم قدم النتائج، ثم نفذ التعديلات، ثم اختبر، ثم commit.**